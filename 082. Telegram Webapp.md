# 创建 Bot

1. 打开 Telegram，搜索并进入 **@BotFather**。
2. 点击 **Start**，发送 `/newbot`。
3. 输入 Bot 的显示名称，例如：`My Web App`。
4. 输入 Bot 的用户名，例如：`my_web_app_bot`。
   - 用户名必须以 `bot` 结尾。
5. 创建成功后，复制并保存 Bot Token。

> 不要公开或提交 Bot Token。

# Telegram Mini App

准备一个已上线的 HTTPS 页面，例如：`https://example.com`。

1. 在 **@BotFather** 中发送 `/newapp`。
2. 选择刚才创建的 Bot。
3. 输入 Mini App 的名称，例如：`My Web App`。
4. 输入一段简短描述。
5. 按提示上传封面或演示素材；暂时没有时，在允许跳过的步骤发送 `/empty`。
6. 输入页面地址，例如：`https://example.com`。
7. 输入短名称，例如：`app`。
8. 创建成功后，保存 BotFather 返回的链接：

```text
https://t.me/my_web_app_bot/app
```

以后需要修改时，向 **@BotFather** 发送 `/myapps`，然后选择对应的 Mini App。

# 本地开发调试

## 1. 安装 cloudflared

```bash
$ brew install cloudflared
```

## 2. 启动本地项目

```bash
$ npm run dev
```

记住 Vite 显示的地址，默认是 `http://localhost:5173`。

## 3. 创建 HTTPS 地址

新开一个终端：

```bash
$ cloudflared tunnel --url http://localhost:5173
```

复制终端生成的地址，例如：

```text
https://example.trycloudflare.com
```

## 4. 配置 Vite

在 `vite.config.ts` 中加入生成的域名，不要包含 `https://`：

```ts
export default defineConfig({
  server: {
    allowedHosts: ['example.trycloudflare.com'],
  },
})
```

## 5. 更新 Mini App 地址

1. 向 **@BotFather** 发送 `/myapps`。
2. 选择 Mini App，按提示修改 Web App URL。
3. 填入刚才生成的完整 HTTPS 地址。
4. 重新打开 Mini App 即可调试。

> Vite 和 cloudflared 两个终端都要保持运行。重新启动 cloudflared 后地址通常会变化，需要同步修改 `allowedHosts` 和 Web App URL。
