Telegram APis： https://core.telegram.org/bots/webapps#initializing-mini-apps

# 创建 Bot

1. 打开 Telegram，搜索并进入 **@BotFather**。
2. 点击左下角的 **Open** 按钮，进入 BotFather 主界面。
3. 点击 **Creeate a New Bot**。
4. 填写信息（如：头像，名称，描述信息以及用户名）
5. Bot 创建完成之后，可以在 **Settings** 菜单编辑相关信息。

# Telegram Mini App

准备一个已上线的 HTTPS 页面，例如：`https://example.com`。

1. 选择刚才创建的 Bot 并进入。
2. 点击 Settings - Mini Apps 进入 Mini Apps 管理页。
3. 点击 Create Direct Link 创建 Mini App，填写相关信息即可。

# 本地开发调试

已 vite-react-ts 模板为例，假设端口号为 5173.

Telegram 需要 https 地址，VS Code  本身已经端口转发能力（当然你也可以选择 cloudflared 或其他内网穿透工具 ），操作如下：

1. 打开底部工具面板：<kbd>Command</kbd> + <kbd>J</kbd>。

2. 选择 **端口**。

3. 点击 **添加端口**，并填写本地开发的 **端口号**，等待生成转发地址。

4. 在 **转发地址** 上鼠标右键选择 **端口可见性** 并将其设置为 **公共** 即可。

5. 在 `vite.config.ts` 中加入生成的域名，不要包含 `https://`：

   ```
   export default defineConfig({
     server: {
       allowedHosts: ['example.trycloudflare.com'],
     },
   })
   ```

6. 将转发地址设置成 Mini App 的 Direct Link 即可访问。
