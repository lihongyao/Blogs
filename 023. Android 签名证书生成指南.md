# 证书

## 1. 生成

Android 应用在发布时需要签名文件，keytool 用于生成签名文件

在终端进入 `keytool` 目录，一般位于：`Android Studio安装目录\jre\bin`

输入如下指令，查看 `keytool` 的参数说明：

 ```shell
 $ keytool -genkey --help
 ```

生成签名语法：

```shell
$ keytool -genkey -v -alias your_alias -keyalg RSA -keysize 2048 -validity 36500 -keystore your_keystore.jks
```

参数说明：

- `-alias`：证书别名（建议使用英文字母和数字）
- `-keyalg RSA`：使用 RSA 加密算法（2025年仍为行业标准）
- `-keysize 2048`：密钥长度2048位（安全性更高）
- `-validity 36500`：有效期100年（避免频繁更新）
- `-keystore`：生成的证书文件名（建议.jks或.keystore后缀）

执行后会提示输入以下信息：

1. 密钥库密码（storePassword）
2. 确认密码
3. 证书密码（keyPassword，建议与密钥库密码相同）
4. 姓名、组织等基本信息（可按需填写）

## 2. 查看

```shell
$ keytool -list -v -keystore xxx.jks 
```

# 获取签名MD5

1、[点击下载签名生成工具 >>](https://res.wx.qq.com/open/zh_CN/htmledition/res/dev/download/sdk/Gen_Signature_Android2.apk)

2、输入包名查询

