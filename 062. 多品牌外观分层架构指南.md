# 概述

本文介绍了一套面向 **多品牌、多布局、多主题、多皮肤** 业务场景的前端架构设计方案。

该方案以 **统一技术栈** 为基础，通过「环境变量 + 文件化配置」实现品牌隔离，借助 React Context 统一管理全局外观状态，并在架构层面明确划分 **布局（Layout）**、**主题（Theme）** 与 **皮肤（Skin）** 的职责边界。

结合动态布局加载机制与主题–皮肤分层策略，最终构建了一套 **结构可切换、视觉可组合、品牌高度解耦** 的前端外观体系。

## 背景与目标

随着业务规模的增长与品牌矩阵的不断扩展，我们需要在 **统一的前端技术栈**（Next.js 16+，App Router）之上，构建一套 **可复用、可扩展、可持续演进** 的多品牌前端架构方案。

在保证技术栈与工程体系统一的前提下，该方案需要同时应对 **页面结构、视觉形态与品牌资源** 等多个维度的差异化需求，并明确不同层级能力的职责边界，以避免随业务演进产生结构混乱与样式耦合问题。

围绕上述目标，整体方案需满足以下核心能力：

☑️ **多品牌支持**

通过配置化与环境隔离的方式实现品牌差异化，避免分支维护与代码分叉，确保品牌能力可扩展、可演进。

☑️ **多布局 × 多主题 × 多皮肤组合**

将页面结构与主题、皮肤进行解耦设计，支持不同布局形态与视觉方案的自由组合，降低复杂场景下的组合成本。

☑️ **资源管理**

支持按【品牌/皮肤】维度加载并隔离图片及静态资源，确保资源体系清晰、可控，且具备良好的扩展性。

## 概念定义

在多品牌体系中，如果外观能力的层级边界不清，容易导致样式耦合、配置混乱以及扩展受限。因此，在具体实现之前，需要对核心概念进行统一约定，以便在架构上明确 **结构（Layout）、形态（Theme）、视觉（Skin）** 与资源（Assets）的职责边界。

| **概念** | **定义**       | **示例**                       | **职责**           |
| -------- | -------------- | ------------------------------ | ------------------ |
| 布局     | 页面结构与排列 | 顶部导航 + 左右布局            | 决定“组件怎么排”   |
| 主题     | 组件形态与密度 | 现代、经典                     | 决定“组件形态”     |
| 皮肤     | 配色与视觉氛围 | 浅色、深色                     | 决定“颜色与氛围”   |
| 套图     | 静态资源集合   | 默认套图、节日套图（跟皮肤走） | 决定“使用哪些资源” |
| 品牌     | 最终组合与约束 | 品牌 A：现代 × 深色 × 套图     | 决定“选择与约束”   |

> 提示：在实现层面，套图通常由品牌或皮肤进行选择或覆盖，但在架构设计上，套图应作为独立维度存在，以避免资源体系与视觉策略产生强耦合。

## Tokens 分层

在前端视觉系统中，Tokens 用于统一管理设计值。可以分为两层：

![](./imgs/skin_tokens_level.jpeg)

- 基础变量（Foundation Tokens）

  - 原子级、稳定的基础设计值（裸值），不随主题或皮肤变化。

  - 包括：颜色尺度、字号刻度、圆角、阴影、间距等。

  - 作用：提供系统稳定底座，让语义变量有可靠来源。

  - 示例：
  
    ```css
    :root {
      --palette-s01-900: #006fff;
      --palette-s02-900: #00983a;
      
      --radius-100: 6px;
      --radius-150: 8px;
      --radius-200: 12px;
      
      --space-100: 4px;
      --space-150: 6px;
      --space-200: 8px;
      ...
    }
    ```
  
- 语义变量（Semantic Tokens）

  - 将基础变量映射为**有语义的变量**，供组件使用。

  - 特点：

    - 组件只依赖语义变量，不直接引用基础变量。
    - 换主题 / 换皮肤 = 修改语义变量的值 → 实现视觉适配。

  - 职责：

    - 提供稳定接口给组件（如 --button-bg、--card-radius）
    - 支持主题 / 皮肤 / 业务组件的差异化
    - 保证组件与基础变量解耦

  - 示例：

    ```css
    :root {
      --button-bg: var(--color-primary-500);
      --button-bg-hover: var(--color-primary-600);
      --button-padding-y: var(--spacing-8);
      --button-padding-x: var(--spacing-16);
      --button-radius: var(--radius-4);
      --button-shadow: var(--shadow-l1);
      ...
    }
    ```

## 主题/皮肤与 Tokens 的关系

职责划分（强约束）

| **层级**      | **关注点**                | **是否改颜色**    |
| ------------- | ------------------------- | ----------------- |
| Theme（主题） | 布局 / 尺寸 / 空间 / 层级 | ❌（特殊情况除外） |
| Skin（皮肤）  | 颜色体系                  | ✅                 |
| Tokens        | 变量映射                  | —                 |

主题和皮肤本质都是 Semantic Tokens 的不同赋值集合

## 组件消费原则

**✅ 允许**

- 组件使用 Semantic Tokens
- 主题 / 皮肤通过覆盖语义变量实现视觉变化

**❌ 禁止**

- 组件直接使用 Foundation Tokens
- 组件中出现硬编码颜色 / 间距 / 圆角
- 在组件中感知「主题 / 皮肤」概念

组件视角：组件不知道什么是主题和皮肤，只知道“我该用哪个语义变量”

## 语义变量命名规范

**原则**

- 以「组件 + 用途」为中心
- 命名稳定，可跨主题、皮肤长期复用

**示例**

```css
--btn-bg
--btn-radius
--card-shadow
--page-header-height
--modal-mask-bg
```

## 套图资源

## 资源目录规范（以 Figma 为例）

```
/**
 * 美术/研发 Collection Name 约定
 * 1. 基础 tokens："primitives-color", "primitives-radius", "primitives-space"
 * 2. 皮肤 tokens: "semantic-color"
 * 3. 主题 tokens: "semantic-radius", "semantic-space"
 * 4. 字体 tokens: "text-style"
 */
```

变量命名规范：

- **统一使用英文小写字母**
- 多单词变量使用 **连字符（-）** 连接
- 禁止使用中文或大小写混用

##  主题 vs 布局

| **类型** | **关注点**             | **示例**           | **核心职责**                                   |
| -------- | ---------------------- | ------------------ | ---------------------------------------------- |
| Theme    | 组件形态 / 间距 / 层级 | classic / modern   | 调整视觉密度 / 组件样式，不改变 DOM 排列       |
| Layout   | 页面骨架 / 排列方式    | top-nav / side-nav | 决定导航 / 内容 /侧边栏如何排列，改变 DOM 结构 |

> ⚠️ 注意：不要把“页面骨架”放到 Theme 里，否则以后换主题就可能要改 DOM，违背了 Theme 的职责。

## 最终愿景

整体上，我们把设计 Tokens 分为两层：

- **基础 Tokens（Base / Primitive Tokens）**
- **语义 Tokens（Semantic Tokens）**

### 基础 Tokens

基础 tokens 本质上就是**具体的色值映射**，只描述“颜色是什么”，不描述“颜色用来干什么”。

- 命名方式并不重要，可以是数字、色阶、或其他规则
- **开发不会、也不应该直接使用基础 tokens**
- 它更像是一个**全局颜色资产库**

这个颜色库**不跟具体项目走**，而是所有项目共享：

- 任意项目新增颜色，都会统一沉淀到这个基础色库中
- 前期由于覆盖面不足，基础 tokens 的增长会比较快
- 随着色库逐渐完善，后期新增色值会越来越少，趋于稳定

在维护方式上，可以按色阶来组织，例如：

- 赤 / 橙 / 黄 / 绿 / 青 / 蓝 / 紫
- 每个颜色下维护若干个不同亮度或饱和度的色阶（比如 10～20 个）

实际设计时，应当**优先从现有色库中选色**；

只有在确实无法满足需求时，才新增一个色阶，并同步沉淀进基础色库。

### 语义 Tokens

语义 tokens 关注的是**“用途和角色”**，而不是视觉表现。

它们应该按**功能语义**来命名，而不是颜色、形状或具体样式。

例如：

```css
--tabbar-icon-default
```

它表达的是：「标签栏中，默认状态下 icon 的颜色」

不管是 X 项目还是 Y 项目，都会存在这个语义。

再举一个例子，比如活动状态标识：

- 在 X 项目中可能是圆点
- 在 Y 项目中可能是三角形

形状不重要，重要的是它的**语义角色**——“标识活动状态”。

因此可以命名为：

```css
--activity-status-idot
```

如果需要区分不同状态，也应该**对齐状态语义，而不是颜色**：

```css
--activity-status-success-idot
--activity-status-fail-idot
```

#### 语义 Tokens 的跨项目对齐策略

语义 tokens 需要在不同项目之间保持**全集对齐**，但允许**增量演进**。

例如：

- X 项目当前有 100 个语义 tokens
- Y 项目在此基础上新增了 20 个

那么最终：

- Y 项目拥有 120 个语义 tokens
- **X 项目也需要同步补齐这 120 个语义 tokens**（即使部分在 X 中暂时不用）

这样做的好处是：

- 任意项目 + 任意皮肤 / 主题，都能完整匹配到语义变量
- 避免因为缺失 token 导致的样式兜底、hack 或逻辑分支
- 为多项目、多皮肤复用提供长期稳定的基础

总结：

> **基础 tokens 是全局颜色资产库，语义 tokens 是跨项目对齐的功能语言层。**

> **基础 tokens 稳定收敛，语义 tokens 增量演进，但全集始终保持一致。**

# 任务目标

本文旨在搭建一套支持**多主题、多皮肤、多品牌**的前端外观体系，并设计一套**与外观体系解耦的图片资源管理方案**，以支持在 **构建时** 灵活切换主题、皮肤及视觉资产组合。

场景设定：

- 框架：Next.js 16（App Router）
- 主题：modern、classic
- 皮肤：light、dark
- 品牌：afun、bfun
- 套图：default ...

> **说明**：本文将提供完整的示例工程结构、关键实现思路及核心代码。部分实现为突出架构设计而做了适当简化，但不影响整体思路的理解与实际落地。

# 分析

围绕多主题、多皮肤、多品牌的目标，整体方案可以拆解为以下几个核心环节：

1. 品牌配置：

   通过配置化方式描述品牌能力，明确每个品牌所使用的主题、皮肤，以及是否允许进行样式或资源覆盖，为品牌差异化提供统一、可扩展的入口。

2. 环境变量：

   通过环境变量 BRAND_NAME 标识当前应用所运行的品牌，用于在构建阶段或运行阶段选择对应的品牌配置。

3. 资源管理：

   对主题、皮肤、品牌覆盖样式及图片资源进行统一规划，集中存放于 @/assets 目录下，并通过模块化方式引入。

   > 提示：不建议将样式资源放置在 public 目录后通过 RootLayout 手动插入 link 标签的方式引入，这种做法会影响关键渲染路径，从而对 LCP 指标产生不利影响。

4. 加载资源：

   根据业务对“是否支持运行时切换”的需求，资源加载可以分为两种实现方案：

   方案A：构建时确定（主题皮肤固定）

   - 在预构建阶段（如 prebuild / predev），根据环境变量读取品牌配置，按品牌生成并导入对应的主题与皮肤样式，适用于主题和皮肤在部署后不需要变更的场景。

   方案B：运行时确定（支持动态切换）

   - 在 SSR 阶段，读取包网配置（Cookie — 配置 — 默认），按需加载主题/皮肤相关资源，支持用户在运行时进行切换。

5. 配置共享：

   基于 React Context 在应用内部共享当前品牌、主题及皮肤配置，确保服务端组件与客户端组件在渲染过程中使用一致的外观状态。

6. 动态切换：

   为避免服务端组件与客户端组件状态不一致，主题或皮肤切换时，仅需将新的配置写入 Cookie 并触发页面刷新，使应用重新走完整的服务端渲染流程，从而保证整体一致性。

7. 套图资源：

   套图（包括 SVG 图标、插画、图片等视觉资产）应作为独立体系存在，与主题和皮肤解耦。组件只需按语义调用所需资源，而不关心其来源。常见管理方式包括：

   - 放在 `@/assets` 通过模块化导入或 `public` 目录中。
   - 如果需要 CDN 加速，可部署到图床或云存储。
   - 也可以保存在版本管理系统（如 SVN），按业务需求进行分层管理。

# 具体实现

## 工程目录结构

以下示例仅展示与 **主题 / 皮肤系统** 相关的关键目录结构，帮助你理解资源的组织方式与加载流程。

> 提示：Next.js 项目的基础搭建过程此处不再赘述，如需参考请查看  [官方文档 ↪](https://nextjs.org/docs/app/getting-started/installation#quick-start)

`public/`

```
public
├── icons/                # SVG
│   ├── google.svg
│   └── ...
└── series/
    ├── s01-blue          # 套图 1
    ├── s02-green         # 套图 2
    ├── ...
    ├── x01-blue          # 套图 3
    ├── x02-green         # 套图 4
    └── ...               # 套图 x
```

`scripts/`

```shell
$ tree scripts
scripts
├── gen-figma-variables
│   ├── index.ts
│   └── type.d.ts
└── prebuild-setup        # 预构建脚本，处理品牌 brand.css，读取品牌信息，写入环境变量
    └── index.ts
```

`src/` 

```
src
├── app
│   └── [lang]                     # 国际化/路由匹配（可忽略）
│       ├── _components            # 页面级组件，便于演示客户端组件和服务端组件
│       │   ├── Card.tsx           # 卡片组件
│       │   ├── ClientComp.tsx     # 客户端组件 —— use client
│       │   └── ServerComp.tsx     # 服务端组件（默认）
│       ├── layout.tsx             # Root Layout
│       └── page.tsx               # Root Page
├── assets
│   └── styles
│       ├── generated              # 品牌样式（预构建时由脚本生成，不支持动态切换）
│       │   └── brand.css.ts       
│       ├── overrides              # 品牌覆盖样式
│       │   └── afun.css
│       ├── skins                  # 皮肤 - 语义化变量（Semantic Tokens）
│       │   ├── dark.css
│       │   └── light.css
│       ├── themes                 # 主题 - 语义化变量（Semantic Tokens）
│       │   ├── classic.css
│       │   └── modern.css
│       └── tokens                 # 基础变量（Foundation Tokens）
│           └── index.css
├── components                     # 公共组件
│   ├── features                   # 业务组件
│   │   └── ThemeSkinSwitcher.tsx  # 主题/皮肤切换器 
│   ├── layout                     # 布局（结构）               
│   │   ├── TopNavLayot.tsx  
│   │   ├── index.tsx          
│   │   └── SideNavLayout.tsx   
│   └── ui                         # 基础组件
├── configs       
│   └── brands                     # 品牌配置相关
│       ├── afun.ts
│       ├── bfun.ts
│       └── types.ts
├── hooks
│   └── useThemeActions.tsx        # 客户端获取/切换主题皮肤的钩子
├── lib
│   └── brand.ts                   # 服务端获取品牌（主题/皮肤）配置工具函数
├── providers
│   └── brand.provider.tsx         # 主题/皮肤上下文（夸组件共享主题/皮肤配置）
└── styles
    └── globals.css
```

## 环境变量

按品牌和环境来定义，例如：

- `.env.afun.dev` `.env.afun.stage` `.env.afun.prod`
- `.env.bfun.dev` `.env.bfun.stage` `.env.bfun.prod`

每个环境变量文件中，需标识当前品牌，以便在构建或运行时获取品牌配置：

```
NEXT_PUBLIC_BRAND_NAME=afun
...
```

```json
"predev": "cross-env dotenv -e env/.env.${app-afun}.dev -- tsx scripts/gen-brand-css/index.ts",
"dev": "cross-env dotenv -e env/.env.${app-afun}.dev -- next dev --turbopack",

"prebuild": "cross-env dotenv -e env/.env.$app.$env -- tsx scripts/gen-brand-css/index.ts",
"build": "cross-env dotenv -e env/.env.$app.$env -- next build --turbopack"
```

```shell
$ [app=afun] pnpm dev
$ [app=afun] env=prod pnpm build
```

## 品牌配置

```shell
$ tree src/configs/brands
src/configs/brands
├── afun.ts
├── bfun.ts
└── types.ts
```

> `src/configs/brands/types.ts`

```ts
// src/configs/brands/types.ts

// -- 品牌
export type BrandName = "afun" | "bfun";
// -- 主题
export type Theme = "classic" | "modern";
// -- 皮肤
export type Skin = "dark" | "light";
// -- 套图
export type Series = "default";
// -- 布局
export type Layout = 'top-nav' | 'side-nav' 

// -- 品牌配置
export type BrandConfig = {
  /** 品牌名称 */
  brandName: BrandName;
  /** 主题 */
  theme: Theme;
  /** 皮肤 */
  skin: Skin;
  /** 套图 */
  series: Series;
  /** 布局 */
  layout: Layout;
  /** 是否启用覆盖 */
  overrides?: boolean;
  /** 扩展字段 */
  [key: string]: unknown;
};

```

> `src/configs/brands/afun.ts`

```ts
// src/configs/brands/afun.ts

import type { BrandConfig } from "./types";

export default {
  brandName: "afun",
  theme: "classic",
  skin: "dark",
  series: 'default',
  overrides: true,
} satisfies BrandConfig;
```

> `src/configs/brands/bfun.ts`

```ts
// src/configs/brands/bfun.ts

import type { BrandConfig } from "./types";

export default {
  brandName: "bfun",
  theme: "modern",
  series: 'default',
  skin: "light",
} satisfies BrandConfig;
```

## 脚本生成

```ts
// scripts/prebuild-setup/index.ts
import fs from "node:fs";
import path from "node:path";
import { fileURLToPath } from "node:url";
import type { BrandConfig } from "@/configs/brands/types";

// === 路径与目录解析 ===
// 当前文件路径
const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);

// 项目根目录
const ROOT_DIR = path.resolve(__dirname, "../../");

// 资源与配置目录
const STYLES_ROOT = path.resolve(ROOT_DIR, "src/assets/styles");
const GENERATED_DIR = path.resolve(STYLES_ROOT, "generated");
const BRAND_CONFIG_DIR = path.resolve(ROOT_DIR, "src/configs/brands");

// === 读取环境变量 ===
const APP = process.env.app ?? "kapokx_br";
const ENV = process.env.env ?? "dev";

console.log(APP, "---", ENV);
if (!APP || !ENV) {
  console.error("❌ 请设置环境变量 app 和 env");
  process.exit(1);
}

// === 动态导入品牌配置 ===
const brandConfigPath = path.resolve(BRAND_CONFIG_DIR, `${APP}.${ENV}.ts`);

let brandConfig: BrandConfig;

try {
  const mod = await import(brandConfigPath);
  brandConfig = mod.default as BrandConfig;
} catch (err) {
  console.error(`❌ 加载品牌配置失败: ${brandConfigPath}`);
  throw err;
}

// === 生成品牌 CSS 文件 ===
const brandCssContent = `/**
 * ⚠️ 自动生成文件
 * 品牌: ${APP}
 * 环境: ${ENV}
 *
 * 请勿手动修改
 */

import "../core/index.css";
import "../themes/${brandConfig.theme}.css";
import "../skins/${brandConfig.skin}.css";
`;

fs.mkdirSync(GENERATED_DIR, { recursive: true });

const brandCssFile = path.resolve(GENERATED_DIR, "brand.css.ts");
fs.writeFileSync(brandCssFile, brandCssContent, "utf8");

console.log("✅ 品牌 CSS 已生成");
console.log("   文件 :", brandCssFile);
console.log("   品牌 :", APP);
console.log("   环境 :", ENV);
console.log("   主题 :", brandConfig.theme);
console.log("   皮肤 :", brandConfig.skin);

// === 写入环境变量文件 (.env) ===
const envOutputFile = path.resolve(ROOT_DIR, ".env");

const envContent = `# === 自动生成，请勿手动修改 ===
NEXT_PUBLIC_THEME=${brandConfig.theme}
NEXT_PUBLIC_SKIN=${brandConfig.skin}
NEXT_PUBLIC_SERIES=${brandConfig.series}
`;

fs.writeFileSync(envOutputFile, envContent, "utf8");

console.log("✅ 品牌配置已写入 .env 文件");
console.log("   文件 :", envOutputFile);
console.log("   NEXT_PUBLIC_THEME  :", brandConfig.theme);
console.log("   NEXT_PUBLIC_SKIN   :", brandConfig.skin);
console.log("   NEXT_PUBLIC_SERIES :", brandConfig.series);

```

## 主题皮肤

> 提示：这里提供的是简单样板配置，仅供参考，不保证运行效果。

### 目录结构

```
$ tree ./src/assets/styles  
./src/assets/styles
├── overrides         # 品牌覆盖样式
│   └── afun.css
├── skins             # 皮肤 —— Semantic Tokens
│   ├── dark.css
│   └── ligtht.css
├── themes            # 主题 —— Semantic Tokens
│   ├── classic.css
│   └── modern.css
└── tokens            # 基础变量 —— Fundation Tokens
    └── index.css
```

> 提示：实际开发中，除了 `overrides` 目录，`skins` `themes` `tokens` 应当通过脚本从 Figma 拉取变量，解析生成，当然，如果不限麻烦，也可以自行通过插件从Figma工具复制粘贴。

### 基础变量

```css
/* public/styles/tokens/index.css */
:root {
  --radius-none: 0px;
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 16px;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;

  --gap-xs: 4px;
  --gap-sm: 8px;
  --gap-md: 16px;
  --gap-lg: 24px;

  --colors-purple-100: #960ce1ff;
  --colors-purple-90: #ad29ffff;
  --colors-purple-80: #a820f2ff;
  --colors-purple-70: #b200ffff;
  --colors-purple-60: #b640ffff;
  --colors-purple-40: #8a1dffff;
  --colors-purple-30: #9837ffff;
  --colors-purple-20: #b36cffff;

  --colors-orange-100: #e55000ff;
  --colors-orange-90: #ff5900ff;
  --colors-orange-80: #ff6600ff;
  --colors-orange-70: #ff7a15ff;
  --colors-orange-60: #ff8622ff;
  --colors-orange-40: #f59b23ff;
  --colors-orange-30: #ffb73cff;
}

```

### 主题

```css
/* public/styles/themes/classic.css */
[data-theme="classic"] {
  --card-radius: var(--radius-none);
  --card-padding: var(--gap-md);
}
```

```css
/* public/styles/themes/modern.css */
[data-theme="modern"] {
  --card-radius: var(--radius-lg);
  --card-padding: var(--gap-lg);
}
```

### 皮肤

```css
/* public/styles/skins/dark.css */
[data-skin="dark"] {
  --card-bg: #1e293b;
  --card-text: #cbd5e1;
  --card-border-color: var(--colors-orange-100);
  --header-border-color: var(--colors-orange-40);
}
```

```css
/* public/styles/skins/light.css */
[data-skin="light"] {
  --card-bg: #ffffff;
  --card-text: #333;
  --card-border-color: var(--colors-purple-100);
  --header-border-color: var(--colors-purple-40);
}
```

### 重写

```css
/* public/styles/overrides/afun.css */
:root {
  --color-primary: red;
}
```

## 布局

多主题模式下，可根据当前主题动态选择对应的布局组件，实现不同风格的页面结构

### 目录结构

```shell
src/components/layout
├── index.tsx
├── SideNavLayout.tsx
└── TopNavLayot.tsx
```

### 代码实现

> 提示：示例代码，仅供参考

```tsx
// src/components/layout/SideNavLayout.tsx
export default function SideNavLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen">
      <header className="h-14 flex items-center border-b px-(--card-padding) border-(--header-border-color)">
        <h1>Classic Layout</h1>
      </header>
      <main className="m-4">{children}</main>
    </div>
  );
}
```

```tsx
// src/components/layout/TopNavLayot.tsx
export default function TopNavLayot({ children }: { children: React.ReactNode }) {
  return (
    <div className="min-h-screen">
      <header className="h-14 flex items-center border-b px-(--card-padding) border-(--header-border-color)">
        <h1 className="tracking-(--gap-sm)">Modern Layout</h1>
      </header>
      <main className="m-4">{children}</main>
    </div>
  );
}
```

```tsx
import dynamic from "next/dynamic";
import type { ComponentType, ReactNode } from "react";
import type { Layout } from "@/configs/brands/types";

export const AppLayouts: Record<Layout, ComponentType<{ children: ReactNode }>> = {
  'top-nav': dynamic(() => import("./TopNavLayot")),
  'side-nav': dynamic(() => import("./SideNavLayout"))
};
```

## 服务端：获取品牌配置

> `src/lib/brand.ts`

```ts
// src/libs/brand.ts

import { cookies } from "next/headers";
import type { BrandConfig } from "@/configs/brands/types";

/**
 * SSR 读取包网信息
 * 优先级：cookie > 环境变量 > 默认包网
 */
export async function getBrandConfigSSR(): Promise<BrandConfig> {
  // 1. 根据环境变量，读取包网配置
  const brandName = process.env.NEXT_PUBLIC_BRAND_NAME;
  const module = await import(`@/configs/brands/${brandName}.ts`);
  const brandConfig = module.default as BrandConfig;
  // 2. 返回包网配置
  return { ...brandConfig };
}
```

## 客户端：共享主题配置

> `src/providers/brand.provider.ts`

```tsx
// src/providers/brand.provider.tsx

// pnpm add js-cookie
// pnpm add @types/js-cookie -D

"use client";
import { createContext, type ReactNode, useContext } from "react";
import type { BrandConfig } from "@/configs/brands/types";

const BrandConfigContext = createContext<BrandConfig | null>(null);

export function BrandConfigProvider({ value, children }: { value: BrandConfig; children: ReactNode }) {
  return (
    <BrandConfigContext.Provider value={value}>
      {children}
    </BrandConfigContext.Provider>
  );
}

export function useBrandConfig() {
  const ctx = useContext(BrandConfigContext);
  if (!ctx) throw new Error("必须在 BrandConfigProvider 上下文中使用 useBrandConfig");
  return ctx;
}

```
## 根布局 

> `src/app/[lang]/layout.tsx`

```tsx
// src/app/[lang]/layout.tsx
import "@/styles/globals.css";

import { ThemeLayouts } from "@/components/layout";
import { getBrandConfigSSR } from "@/lib/brand";
import { BrandConfigProvider } from "@/providers/brand.provider";

type Props = {
  children: React.ReactNode;
  params: Promise<{ lang: string }>;
};

export const runtime = "edge";
export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
  minimumScale: 1,
  maximumScale: 1,
  userScalable: false,
  viewportFit: "cover",
};

export default async function RootLayout({ children, params }: Props) {
  // 获取当前语言
  const { lang } = await params;
  ...
  
  // SSR 获取品牌配置
  const brand = await getBrandConfigSSR();
  
  // 基于品牌配置获取主题布局
  const Layout = ThemeLayouts[brand.theme];

  return (
    <html lang={lang} data-theme={brand.theme} data-skin={brand.skin}>
      <head>
        <meta name="viewport" content="width=device-width,initial-scale=1.0,viewport-fit=cover,minimum-scale=1,maximum-scale=1,user-scalable=no" />
      </head>
      <body className="w-full min-h-screen flex justify-center items-center">
        <BrandConfigProvider initialBrand={brand}>
          <Layout>{children}</Layout>
        </BrandConfigProvider>
      </body>
    </html>
  );
}
```

## 业务组件

展示 token/skin 的使用

> `src/app/[lang]/_components/ClientInfo.tsx`

```tsx
// src/app/[lang]/_components/ClientInfo.tsx
"use client";
import { useBrandConfig } from "@/providers/brand.provider";

export default function ClientInfo() {
  const brand = useBrandConfig();
  return (
    <div>
      <h2 className="mb-4">—— 客户端组件 ——</h2>
      <h2>当前主题: {brand.theme}</h2>
      <h2>当前皮肤: {brand.skin}</h2>
      <h2>当前布局: {brand.layout}</h2>
      <h2>当前品牌: {brand.brandName}</h2>
    </div>
  );
}
```

> `src/app/[lang]/_components/ServerInfo.tsx`

```tsx
// src/app/[lang]/_components/ServerInfo.tsx

import { getBrandConfigSSR } from "@/lib/brand";

export default async function ServerInfo() {
  const brand = await getBrandConfigSSR();
  return (
    <div>
      <h2 className="mb-4">—— 服务端组件 ——</h2>
      <h2>当前主题: {brand.theme}</h2>
      <h2>当前皮肤: {brand.skin}</h2>
      <h2>当前布局: {brand.layout}</h2>
      <h2>当前品牌：{brand.brandName}</h2>
    </div>
  );
}

```

```tsx
// src/app/[lang]/_components/Card.tsx
import Image from "next/image";
import { getBrandConfigSSR } from "@/libs/brand";
import { getImgUrl } from "@/libs/utils";
import ClientInfo from "./ClientInfo";
import ServerInfo from "./ServerInfo";

export default async function Card() {
  const brand = await getBrandConfigSSR();
  return (
    <div className="flex gap-4 border border-(--card-border-color)  rounded-(--card-radius) p-(--card-padding) bg-(--card-bg) text-(--card-text)">
      <Image width={300} height={200} src={getImgUrl(brand, "banner.jpg")} alt="banner" priority />
      <div className="space-y-4">
        <p>Example card. Colors and radius come from theme/skin tokens.</p>
        <button type="button" className="px-4 py-2 inline-block text-sm bg-(--color-primary) text-white">Primary</button>
        <div className="flex items-center gap-4">
          <ClientInfo />
          <div className="h-10 w-px bg-gray-400 mx-4"></div>
          <ServerInfo />
        </div>
      </div>
    </div>
  );
}

```

## 页面调用

```tsx
// src/app/[lang]/page.tsx
import ThemeSkinSwitcher from "@/components/features/ThemeSkinSwitcher";
import ClientInfo from "./_components/ClientInfo";
import ServerInfo from "./_components/ServerInfo";
import ServerInfo from "./_components/Card";

export default async function Page() {
  return (
    <div>
      <ThemeSkinSwitcher />
      <Card />
    </div>
  );
}
```

# 扩展 · 主题皮肤切换

在 Next.js（App Router）中实现主题与皮肤切换时，为保证 **服务端组件（RSC）与客户端组件（Client Component）之间的表现一致性**，通常需要在用户切换主题 / 皮肤后，将选择结果持久化（如写入 Cookie），并通过 **页面刷新** 的方式触发服务端重新渲染。

基于这一前提，现有实现需要在以下几个方面进行调整与补充。

## 1️⃣ 样式加载策略调整

由于 Next.js 对 **运行时动态导入 CSS** 存在限制，当前方案采用在 **构建阶段生成 brand.css**，并在 layout 中直接引入的方式加载样式。

为满足 **运行时切换主题 / 皮肤** 的需求，同时避免影响 **LCP 等性能指标**，主题与皮肤相关的样式不再拆分为多个 CSS 文件，而是将所有视觉 Tokens 统一收敛到一个样式文件中：

```
@/assets/styles/brand.css
```

layout 中的样式引入方式保持不变，通过 **变量或选择器切换** 实现不同主题 / 皮肤的表现，从而避免运行时加载额外样式资源。

## 2️⃣ 服务端品牌配置读取逻辑调整

在服务端渲染阶段，品牌配置的读取需要同时考虑 **默认配置** 与 **用户偏好**。

具体策略如下：

- 优先根据环境变量读取当前品牌的基础配置。
- 再从 Cookie 中读取用户当前选择的主题 / 皮肤。
- 若 Cookie 中不存在对应值，则回退至品牌默认配置。

```ts
// src/libs/brand.ts

import { cookies } from "next/headers";
import type { BrandConfig, Layout, Skin, Theme } from "@/configs/brands/types";

/**
 * SSR 读取包网信息
 * 优先级：cookie > 环境变量 > 默认包网
 */
export async function getBrandConfigSSR(): Promise<BrandConfig> {
  // 1. 根据环境变量，读取品牌配置
  const brandName = process.env.NEXT_PUBLIC_BRAND_NAME;
  const module = await import(`@/configs/brands/${brandName}.ts`);
  const brandConfig = module.default as BrandConfig;
  // 2. 根据 cookie 读取品牌配置
  const cookieStore = await cookies();
  const theme = cookieStore.get("theme")?.value as Theme | undefined;
  const skin = cookieStore.get("skin")?.value as Skin | undefined;
  const layout = cookieStore.get("layout")?.value as Layout | undefined;
  // 3. 返回包网配置
  return {
    ...brandConfig,
    theme: theme ?? brandConfig.theme,
    skin: skin ?? brandConfig.skin,
    layout: layout ?? brandConfig.layout,
  };
}
```

该逻辑确保：

- **首次访问** 使用品牌默认主题 / 皮肤。
- **用户切换后刷新页面**，服务端即可感知并返回一致的渲染结果。
- 避免 SSR / CSR 样式不一致问题。

## 3️⃣ 客户端主题 / 皮肤切换能力封装

在客户端侧，通过封装 `useThemeActions` Hook，统一管理主题与皮肤的切换行为。

切换逻辑主要包括两步：

1. 将用户选择写入 Cookie，用于服务端识别；
2. 主动刷新页面，触发服务端重新渲染。

```tsx
// src/hooks/useThemeActions.ts
import Cookies from "js-cookie";
import type { Layout, Skin, Theme } from "@/configs/brands/types";

export function useThemeActions() {
  const setSkin = (newSkin: Skin) => {
    Cookies.set("skin", newSkin, { path: "/", expires: 365 });
    window.location.reload();
  };
  const setTheme = (newTheme: Theme) => {
    Cookies.set("theme", newTheme, { path: "/", expires: 365 });
    window.location.reload();
  };

  const setLayout = (newLayout: Layout) => {
    Cookies.set("layout", newLayout, { path: "/", expires: 365 });
    window.location.reload();
  };
  return { setSkin, setTheme, setLayout };
}
```

该方式虽然引入了刷新行为，但换来的是：

- RSC 与 Client Component 的完全一致性；
- 更简单、可控的状态来源；
- 更符合 Next.js 当前的推荐使用模型。

## 4️⃣ 主题 / 皮肤切换组件

在此基础上，提供一个纯客户端的 ThemeSkinSwitcher 组件，用于演示主题与皮肤的切换效果。

组件职责保持单一：

- 读取当前品牌上下文中的主题 / 皮肤状态；
- 触发对应的切换动作；
- 不直接关心样式实现细节。

```tsx
// src/components/features/ThemeSkinSwitcher.tsx
"use client";

import clsx from "clsx";
import { useThemeActions } from "@/hooks/useThemeActions";
import { useBrandConfig } from "@/providers/brand.provider";

export default function ThemeSkinSwitcher() {
  const { setTheme, setSkin, setLayout } = useThemeActions();
  const { theme, skin, layout } = useBrandConfig();
  return (
    <div className="mb-4">
      <div className="flex items-center gap-4">
        <span>主题切换：</span>
        <button 
          className={clsx("cursor-pointer", { "text-(--color-primary)": theme === "modern" })}
          onClick={() => setTheme("modern")}
        >
          Modern
        </button>
        <button
          className={clsx("cursor-pointer", { "text-(--color-primary)": theme === "classic" })}
          onClick={() => setTheme("classic")}
        >
          Classic
        </button>
      </div>
      <div className="flex items-center gap-4">
        <span>皮肤切换：</span>
        <button
          className={clsx("cursor-pointer", { "text-(--color-primary)": skin === "light" })}
          onClick={() => setSkin("light")}
        >
          Light
        </button>
        <button
          className={clsx("cursor-pointer", { "text-(--color-primary)": skin === "dark" })}
          onClick={() => setSkin("dark")}
        >
          Dark
        </button>
      </div>
       <div className="flex items-center gap-4">
        <span>布局切换：</span>
        <button
          className={clsx("cursor-pointer", {  "text-(--color-primary)": layout === "top-nav"})}
          onClick={() => setLayout("top-nav")}
        >
          top-nav
        </button>
        <button
          className={clsx("cursor-pointer", {  "text-(--color-primary)": layout === "side-nav"})}
          onClick={() => setLayout("side-nav")}
        >
          side-nav
        </button>
      </div>
    </div>
  );
}

```

## 5️⃣ 移除预构建指令

在引入统一的样式加载与主题 / 皮肤切换方案后，样式资源已不再依赖运行时或构建前的动态生成逻辑，因此可以安全移除 `package.json` 中的 `prebuild` 与 `predev` 脚本指令。

# 研发维护指南（面向开发）

**新增主题**

1. 在 public/styles/themes 目录按现有格式创建新主题文件
2. 在 src/configs/brands/types.ts 为 Theme 添加新类型

**新增皮肤**

1. 在 public/styles/skins 目录按现有格式创建新皮肤文件。
2. 在 src/configs/brands/types.ts 为 Skin 添加新类型。

**新增品牌**

1. 在 src/configs/brands 目录按品牌名称创建配置文件。
2. 在 src/configs/brands/types.ts 为 BrandId 添加新类型。
3. 为新品牌创建环境变量文件，并指定 NEXT_PUBLIC_BRAND_ID。

注意事项：

1. **遵循命名规范**：文件和目录保持与现有格式一致，避免随意命名，保证资源能正确加载。
2. **品牌标识完整**：确保所有环境下的环境变量、配置文件和构建脚本中品牌 ID 一致，保证 SSR 与 CSR 都能正确读取。
3. **类型安全**：在 brands/types.ts 补齐类型，利用 TypeScript 校验防止配置错误。
4. **保持一致性**：文件命名、目录层级和覆盖文件（overrides）统一，避免资源查找失败或重复冗余。
