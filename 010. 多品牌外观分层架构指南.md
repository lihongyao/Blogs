# 概述

本文介绍面向 **跨端（H5/PC）多品牌、多布局、多主题、多皮肤** 的前端外观架构：在统一技术栈上，用 **环境变量 + 品牌配置文件** 做品牌隔离，用 **Tokens 分层 + 布局/主题/皮肤职责分离** 做视觉组合，实现结构可切换、品牌解耦、跨端一致配置。

**设计要点：**

- **配置驱动**：品牌配置统一指定应用名、布局、主题、皮肤；布局在 H5/PC 的差异（如侧栏 vs 底栏）由实现层按端类型适配，配置不改。
- **布局 / 主题 / 皮肤解耦**：布局管整体结构，主题管组件形态与间距，皮肤管颜色；三者可自由组合，主题可与布局关联但不强依赖。
- **Tokens 分层**：基础变量（Foundation）由设计维护，语义变量（Semantic）供组件消费；换主题/皮肤只改语义变量赋值，组件不感知具体主题/皮肤。

> 适用场景：同质化项目（数据一致，仅排版/外观不同）。若业务形态完全不同（如电商 vs 短视频），则不适合。

## 背景与目标

在 **统一技术栈**（如 Next.js 16+ App Router）上支撑多品牌、跨端、多套视觉，需要：**品牌配置化**（避免分支与代码分叉）、**跨端统一配置**（布局类型统一指定，H5/PC 表现由实现层适配）、**布局×主题×皮肤自由组合**、**资源按主题/皮肤维度隔离**。下文通过概念定义、Tokens 分层、职责边界与规范落地上述目标。

## 概念定义

统一约定 **布局、主题、皮肤、资源、品牌** 的含义，避免层级混淆与样式耦合。

| **维度** | **决定**     | **示例**                                                     | **说明**                                                               |
| -------- | ------------ | ------------------------------------------------------------ | ---------------------------------------------------------------------- |
| 布局     | Layout       | side-nav / top-nav                                           | 应用根布局类型；<br />实现层按 H5/PC 适配具体表现（如底栏/侧栏）       |
| 主题     | Theme        | classic / modern                                             | 组件形态、密度、间距；<br />不包含布局结构，可推荐某布局               |
| 皮肤     | Skin         | green / blue                                                 | 视觉风格：颜色、材质                                                   |
| 套图     | Theme + Skin | classic/green/brandA/logo.png、classic/green/common/card.png | 按主题/皮肤目录组织；<br />区分公共资源与品牌特有资源                  |
| 品牌     | 配置实体     | brandA = { appName, layout, theme, skin, overrides? }        | 配置文件定义名称与布局/主题/皮肤组合；<br />可含品牌覆盖样式与资源路径 |

## Tokens 分层

设计值统一用 Tokens 管理，分两层：**基础变量** 由设计维护，**语义变量** 供组件消费，组件不直接依赖基础变量。（可参考 [Radix Themes Tokens](https://www.figma.com/community/file/1280428825266545105/radix-themes)）

![](./imgs/skin_tokens_level.jpeg)

**基础变量（Foundation Tokens）**

基础变量是设计系统的底层原始值，如颜色、字号、圆角、阴影和间距，统一维护并供语义变量引用。

在维护基础变量时，建议按 **色阶** 维护，如下图所示：

![](./IMGS/base-color-tokens-scale.png)

基础变量的命名，可以按色阶定义，固定格式为：

```
--color-{色系}-{色阶}
```

比如：

```css
:root {
  --color-red-050: #FFD6D6;
  --color-red-100: #FF9A9A;
  --color-red-300: #FF5B5B;
  --color-red-500: #E93636;
  --color-red-700: #B9162B;

  --color-green-050: #D7F7E2;
  --color-green-200: #88E1A4;
  --color-green-500: #36C871;
  --color-green-700: #088A4B;

  --color-blue-050: #D9EEFF;
  --color-blue-100: #91C8FF;
  --color-blue-300: #3A8DFF;
  --color-blue-500: #0D5FEA;
  --color-blue-700: #003FA8;
  --color-blue-800: #00235E;
  --color-blue-950: #00132F;
  
  ...
}
```

这样命名可以统一不同色系的色阶，并保留扩展空间：数字越小表示越浅，数字越大表示越深，`500` 作为核心色。新增色系时沿用相同格式，例如 `--color-purple-500`、`--color-orange-500`。

透明度建议单独表达，不要和明暗色阶混在一起：

```
--color-red-500-alpha-50: rgb(233 54 54 / 0.5);
```

**语义变量（Semantic Tokens）**

对基础变量的语义映射；换主题/皮肤只改语义变量取值，组件接口不变，与基础变量解耦。

```css
/* 基础变量（设计维护） */
:root {
  --palette-s01-900: #006fff;
  --radius-100: 6px;
  --space-100: 4px;
  ...
}

/* 语义变量（组件只引用此类） */
:root {
  --button-bg: var(--color-primary-500);
  --button-radius: var(--radius-4);
  ...
}
```

## 职责边界

| **层级** | **关注点**             | **是否改色** | **职责**                                                                 |
| -------- | ---------------------- | ------------ | ------------------------------------------------------------------------ |
| Layout   | 根布局 / 整体框架      | —            | 整体结构（导航、侧栏、底栏等）；配置只写布局类型，H5/PC 表现由实现层适配 |
| Theme    | 组件形态 / 间距 / 密度 | ❌           | 组件骨架与间距；可推荐某布局，不包含布局结构                             |
| Skin     | 颜色与材质             | ✅           | 视觉风格                                                                 |
| Tokens   | 变量映射               | —            | 主题、皮肤均为语义变量的不同赋值集合                                     |

## 跨端适配策略

品牌配置 **不区分端**：同一份配置写清 appName、layout、theme、skin 等；

布局类型（如 `side-nav`）在实现层按端类型映射为具体表现（H5 底栏、PC 侧栏）。主题与皮肤在 H5/PC 共用，无需按端再配。

```typescript
// src/configs/brands/afun.ts
export default {
  appName: "A-FUN",
  theme: "classic",
  skin: "green",
  layout: "side-nav", // 实现层按 H5/PC 适配为底栏/侧栏
  overrides: true,
} satisfies BrandConfig;
```

## Tokens 使用规范

**组件消费**

1️⃣ 只使用语义变量；

2️⃣ 禁止直接使用基础变量或硬编码颜色/间距/圆角；

3️⃣ 组件不感知「主题/皮肤」，只认语义变量名。主题/皮肤通过覆盖语义变量实现变化。

**语义变量命名**

1️⃣ 以「组件 + 用途」为中心，命名稳定、可跨主题/皮肤复用。

2️⃣ 示例：`--btn-bg`、`--btn-radius`、`--card-shadow`、`--page-header-height`、`--modal-mask-bg`。

> 📒 变量技巧：
>
> 对于单背景，渐变背景（涉及上下，左右，角度方向），不要直接衍生，如：
> ```
> --box-bg
> --box-linear-top
> --box-linear-bottom
> --box-linear-left
> --box-linear-right
> ...
> ```
>
> 这样会导致冗余，可以通过更通用的命名，如：
>
> ```
> --box-bg
> --box-linear-[from/start]
> --box-linear-[to/end]
> ```
>
> 如果涉及到三种颜色渐变，中间变量可使用 `--box-linear-via` （参考 tailwindcss 实现思路）

## 资源管理规范

**图片**

1️⃣ 按 `<主题>/<皮肤>/[common|品牌名]` 组织。

2️⃣ 主题区分尺寸比例，皮肤区分色调。

3️⃣ 公共图放 `common`，品牌特有（如 logo）按品牌目录存放。

**Figma**

1️⃣ Collection 建议

- 基础 tokens：`primitives-color` `primitives-radius` `primitives-space`
- 皮肤：`semantic-color`
- 主题：`semantic-radius` `semantic-space`
- 字体：`text-style`，下分 `pc-*` `tablet-*` `h5-*`

2️⃣ 变量命名：英文小写、多词用连字符（-），禁止中文与大小写混用。

## 美术注意

1️⃣ 不按品牌设计，品牌由配置组合而成，设计侧统一维护一套 Tokens 与组件库，各品牌是同一套组件的不同组合与配置。

2️⃣ 公共 Tokens 随项目积累趋于稳定；新品牌/新主题不满足时做增量扩展，保证同一套基础与语义变量可复用于多品牌。

3️⃣ 设计某皮肤（如绿色）时需考虑所有组件与页面在该皮肤下的表现，避免只针对单页或单组件。

# 任务目标

搭建一套支持 **多主题、多皮肤、多品牌** 的前端外观体系，支持在 **构建时** 灵活切换主题、皮肤及视觉资产组合。

**场景设定：**

- 框架：Next.js 16（App Router）
- 主题：`classic`、`modern`
- 皮肤：`green`、`blue`
- 品牌：`afun`、`bfun`

> **说明**：本文提供完整的示例工程结构、关键实现思路及核心代码。部分实现为突出架构设计做了适当简化，不影响整体思路的理解与实际落地。

# 分析

围绕 **跨端多品牌、多布局、多主题、多皮肤** 的目标，整体方案可拆解为以下核心环节：

**1. 品牌配置**

- 通过配置文件定义品牌能力，明确每个品牌使用的 **应用名称、布局、主题、皮肤** 组合
- 品牌配置统一指定布局类型，布局在 H5/PC 的具体表现由实现层根据端类型判断处理
- 可包含品牌特有的覆盖样式和资源，为品牌差异化提供统一入口

**2. 环境变量**

- 通过环境变量 `NEXT_PUBLIC_BRAND` 标识当前品牌，用于构建或运行阶段选择对应配置
- 实现层根据端类型（H5/PC）自动适配布局的具体表现

**3. 资源管理**

- 对布局、主题、皮肤、品牌覆盖样式及图片资源统一规划，集中存放于 `@/assets` 目录（可放SVN 走CDN）
- 通过模块化方式引入，避免影响关键渲染路径（LCP 指标）
- 提示：不建议将样式资源放在 `public` 目录后通过 RootLayout 手动插入 link 标签，会影响 LCP 指标

**4. 资源加载策略**

- **方案A：构建时确定**（布局/主题/皮肤固定）
  - 在预构建阶段（prebuild/predev），根据环境变量读取品牌配置，按品牌生成并导入对应的布局、主题与皮肤样式
  - 适用于部署后不需要变更的场景
- **方案B：运行时确定**（支持动态切换）
  - 在 SSR 阶段，读取用户配置（Cookie → 配置 → 默认），按需加载布局/主题/皮肤相关资源
  - 支持用户在运行时进行切换

**5. 配置共享**

- 基于 React Context 在应用内部共享当前品牌、布局、主题及皮肤配置
- 确保服务端组件与客户端组件渲染过程中使用一致的外观状态

**6. 动态切换**

- 布局/主题/皮肤切换时，将新配置写入 Cookie 并触发页面刷新
- 使应用重新走完整 SSR 流程，保证服务端与客户端状态一致性

**7. 套图资源**

- 套图（SVG 图标、插画、图片等）按 **主题/皮肤/品牌** 维度组织
- 目录结构：`<主题>/<皮肤>/<公共部分>+<品牌特有>`
- 主题区分：不同主题的尺寸比例可能不一致
- 皮肤区分：不同皮肤下图片色调可能不同
- 品牌区分：部分图片带有品牌 logo，公共资源放 `common`，品牌特有按品牌标识组织
- 组件按语义调用所需资源，管理方式：`@/assets` 模块化导入、`public` 目录、CDN 图床/云存储

# 具体实现

## 工程目录结构

以下示例仅展示与 **主题 / 皮肤系统** 相关的关键目录结构，帮助你理解资源的组织方式与加载流程。

> 提示：Next.js 项目的基础搭建过程此处不再赘述，如需参考请查看 [官方文档 ↪](https://nextjs.org/docs/app/getting-started/installation#quick-start)

`public/`

```shell
$ tree public/images
public/images
├── classic            # 主题维度
│   ├── blue           # 皮肤维度
│   │   ├── afun       # 品牌特有资源
│   │   ├── bfun
│   │   └── common     # 公共资源
│   └── green
│       ├── afun
│       ├── bfun
│       └── common
└── modern
    ├── blue
    │   ├── afun
    │   ├── bfun
    │   └── common
    └── green
        ├── afun
        ├── bfun
        └── common
```

`scripts/`

```shell
$ tree scripts
scripts
├── gen-figma-variables
│   ├── index.ts
│   └── type.d.ts
└── prebuild-setup        # 预构建脚本，处理品牌 brand.css，读取品牌信息
    └── index.ts
```

`src/`

```
src
├── app
│   └── [lang]                     # 国际化/路由匹配（可忽略）
│       ├── _components            # 页面级组件，便于演示客户端组件和服务端组件
│       │   ├── Card.tsx           # 卡片组件
│       │   ├── ClientComp.tsx     # 客户端组件 —— use client
│       │   └── ServerComp.tsx     # 服务端组件（默认）
│       ├── layout.tsx             # Root Layout
│       └── page.tsx               # Root Page
├── assets
│   └── styles
│       ├── generated              # 品牌样式（预构建时由脚本生成，不支持动态切换）
│       │   └── brand.css.ts
│       ├── overrides              # 品牌覆盖样式
│       │   └── afun.css
│       ├── skins                  # 皮肤 - 语义化变量（Semantic Tokens）
│       │   ├── dark.css
│       │   └── light.css
│       ├── themes                 # 主题 - 语义化变量（Semantic Tokens）
│       │   ├── classic.css
│       │   └── modern.css
│       └── tokens                 # 基础变量（Foundation Tokens）
│           └── index.css
├── components                     # 公共组件
│   ├── features                   # 业务组件
│   │   └── ThemeSkinSwitcher.tsx  # 主题/皮肤切换器
│   ├── layout                     # 布局（结构）
│   │   ├── TopNavLayot.tsx
│   │   ├── index.tsx
│   │   └── SideNavLayout.tsx
│   └── ui                         # 基础组件
├── configs                        # 品牌配置相关
│   └── brands
│       ├── afun.ts
│       ├── bfun.ts
│       └── types.ts
├── hooks
│   └── useThemeActions.tsx        # 客户端获取/切换主题皮肤的钩子
├── libs
│   └── brand.ts                   # 服务端获取品牌（主题/皮肤）配置工具函数
├── providers
│   └── brand.provider.tsx         # 主题/皮肤上下文（夸组件共享主题/皮肤配置）
└── styles
    └── globals.css
```

## 环境变量

按品牌和环境来定义，例如：

- `.env.afun.dev` `.env.afun.stage` `.env.afun.prod`
- `.env.bfun.dev` `.env.bfun.stage` `.env.bfun.prod`

每个环境变量文件中，需标识当前品牌，以便在构建或运行时获取品牌配置：

```
NEXT_PUBLIC_BRAND=afun
...
```

```json
"predev": "cross-env app=${app:-afun} env=${env:-dev} dotenv -e env/.env.${app-afun}.${env-dev} -- tsx scripts/pre-setup/index.ts",
"dev": "dotenv -e env/.env.${app-afun}.${env-dev} -- next dev --turbopack",

"prebuild": "cross-env app=${app:-afun} env=${env:-dev} dotenv -e env/.env.${app-afun}.${env-dev} -- tsx scripts/pre-setup/index.ts",
"build": "dotenv -e env/.env.${app-afun}.${env-dev} -- next build --turbopack",
```

```shell
$ [app=xxx env=xxx] pnpm dev
$ [app=xxx env=xxx] pnpm build
```

## 品牌配置

```shell
$ tree src/configs/brands
src/configs/brands
├── afun.ts
├── bfun.ts
├── index.ts # 该文件由 scripts/pre-setup/index.ts 脚本生成，需在 .gitignore 忽略
└── types.ts
```

```ts
// src/configs/brands/types.ts
// -- 主题
export type Theme = "classic" | "modern";
// -- 皮肤
export type Skin = "green" | "blue";
// -- 布局
export type Layout = "top-nav" | "side-nav";

// -- 品牌配置
export type BrandConfig = {
  /** 应用名称 */
  appName: string;
  /** 主题 */
  theme: Theme;
  /** 皮肤 */
  skin: Skin;
  /** 布局 */
  layout: Layout;
  /** 是否启用覆盖 */
  overrides?: boolean;
  /** 扩展字段 */
  [key: string]: unknown;
};
```

```ts
// src/configs/brands/afun.ts
import type { BrandConfig } from "./types";

export default {
  appName: "A-FUN",
  theme: "classic",
  skin: "blue",
  layout: "side-nav",
  overrides: true,
} satisfies BrandConfig;
```

```ts
// src/configs/brands/bfun.ts
import type { BrandConfig } from "./types";

export default {
  appName: "B-Fun",
  theme: "modern",
  layout: "top-nav",
  skin: "green",
  overrides: false,
} satisfies BrandConfig;
```

## 脚本生成

```ts
import * as fs from 'node:fs';
import * as path from 'node:path';
import { fileURLToPath } from 'node:url';

import { BrandConfig } from '@/configs/brands/types';

const app = process.env.app;
const env = process.env.env;

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const SCRIPT_NAME = 'scripts/pre-setup/index.ts';
const ROOT = path.resolve(__dirname, '../../');

console.log('pre-setup app:', app);
console.log('pre-setup env:', env);
console.log('pre-setup ...');
console.log('process.env.NEXT_PUBLIC_BRAND >> :', process.env.NEXT_PUBLIC_BRAND);

(async function main() {
  try {
    const brandConfig = await getBrandConfig();
    setupBrandConfig();
    setupBrandCssImports(brandConfig);
  } catch (e) {
    console.error(e);
    process.exit(1);
  }
})();

/**
 * 获取品牌配置
 * @returns
 */
async function getBrandConfig() {
  const brandsIndexPath = path.join(ROOT, '/src/configs/brands/index.ts');
  try {
    const mod = await import(brandsIndexPath);
    return mod.default as BrandConfig;
  } catch (err) {
    console.error(`❌ 加载品牌配置失败: ${brandsIndexPath}`);
    throw err;
  }
}
/**
 * 设置品牌配置
 */
function setupBrandConfig() {
  const brandsIndexPath = path.join(ROOT, '/src/configs/brands/index.ts');
  const brandsDir = path.dirname(brandsIndexPath);
  const brandFilePath = path.join(brandsDir, `${app}.ts`);

  if (!fs.existsSync(brandFilePath)) {
    throw new Error(`[pre-setup] 品牌配置不存在: src/configs/brands/${app}.ts，请先添加该文件`);
  }
  const generatedAt = new Date().toISOString();
  const fileContent = `// 该文件由 ${SCRIPT_NAME} 脚本生成，请勿手动修改
// 生成时间: ${generatedAt}
export { default } from './${app}';`;

  fs.writeFileSync(brandsIndexPath, fileContent, 'utf-8');

  console.log('[pre-setup] 已写入 src/configs/brands/index.ts -> export from %s', app);
}

function setupBrandCssImports(brandConfig: BrandConfig) {
  const generatedAt = new Date().toISOString();
  const brandCssContent = `/**
* 该文件由 ${SCRIPT_NAME} 自动生成，请勿手动修改
*  
* 品牌: ${app}
* 环境: ${env}
* 生成时间: ${generatedAt}
*/

import "../core/index.css";
import "../themes/${brandConfig.theme}.css";
import "../skins/${brandConfig.skin}.css";
`;

  const stylesRoot = path.resolve(ROOT, 'src/assets/styles');
  const generatedDir = path.resolve(stylesRoot, 'generated');

  if (!fs.existsSync(generatedDir)) fs.mkdirSync(generatedDir, { recursive: true });

  const brandCssFilePath = path.resolve(generatedDir, 'brand.css.ts');
  fs.writeFileSync(brandCssFilePath, brandCssContent, 'utf8');
  console.log('[pre-setup] 已写入 src/assets/styles/generated/brand.css.ts');
}

```

## 主题皮肤

> 提示：这里提供的是简单样板配置，仅供参考，不保证运行效果。

### 目录结构

```
$ tree ./src/assets/styles
./src/assets/styles
├── core              # 基础变量 —— Fundation Tokens
│   └── index.css
├── skins             # 皮肤 —— Semantic Tokens
│   ├── blue.css
│   └── green.css
└── themes            # 主题 —— Semantic Tokens
    ├── classic.css
    └── morderen.css
```

> 提示：在实际开发中，除 overrides 目录外，skins、themes、tokens 等目录建议通过脚本从 Figma 拉取设计变量并自动解析生成，以保证设计与代码的一致性和可维护性。如果项目规模较小或对自动化要求不高，也可以通过 Figma 插件手动复制变量进行维护，但长期来看更推荐自动化方案。

### 基础变量

```css
/* public/styles/tokens/index.css */
:root {
  --radius-none: 0px;
  --radius-sm: 4px;
  --radius-md: 8px;
  --radius-lg: 16px;

  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;

  --gap-xs: 4px;
  --gap-sm: 8px;
  --gap-md: 16px;
  --gap-lg: 24px;

  --colors-purple-100: #960ce1ff;
  --colors-purple-90: #ad29ffff;
  --colors-purple-80: #a820f2ff;
  --colors-purple-70: #b200ffff;
  --colors-purple-60: #b640ffff;
  --colors-purple-40: #8a1dffff;
  --colors-purple-30: #9837ffff;
  --colors-purple-20: #b36cffff;

  --colors-orange-100: #e55000ff;
  --colors-orange-90: #ff5900ff;
  --colors-orange-80: #ff6600ff;
  --colors-orange-70: #ff7a15ff;
  --colors-orange-60: #ff8622ff;
  --colors-orange-40: #f59b23ff;
  --colors-orange-30: #ffb73cff;
}
```

### 主题

```css
/* public/styles/themes/classic.css */
[data-theme="classic"] {
  --card-radius: var(--radius-none);
  --card-padding: var(--gap-md);
}
```

```css
/* public/styles/themes/modern.css */
[data-theme="modern"] {
  --card-radius: var(--radius-lg);
  --card-padding: var(--gap-lg);
}
```

### 皮肤

```css
/* public/styles/skins/dark.css */
[data-skin="dark"] {
  --card-bg: #1e293b;
  --card-text: #cbd5e1;
  --card-border-color: var(--colors-orange-100);
  --header-border-color: var(--colors-orange-40);
}
```

```css
/* public/styles/skins/light.css */
[data-skin="light"] {
  --card-bg: #ffffff;
  --card-text: #333;
  --card-border-color: var(--colors-purple-100);
  --header-border-color: var(--colors-purple-40);
}
```

### 重写

```css
/* public/styles/overrides/afun.css */
:root {
  --color-primary: red;
}
```

## 布局

可根据当前布局动态选择对应的应用布局，实现不同风格的页面结构。

### 目录结构

```shell
$ tree src/components/layouts
src/components/layouts
├── index.ts
├── side-nav
│   └── index.tsx
└── top-nav
    └── index.tsx
```

### 代码实现

> 提示：示例代码，仅供参考

```tsx
// src/components/layout/SideNavLayout.tsx
export default function SideNavLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen">
      <header className="h-14 flex items-center border-b px-(--card-padding) border-(--header-border-color)">
        <h1>Classic Layout</h1>
      </header>
      <main className="m-4">{children}</main>
    </div>
  );
}
```

```tsx
// src/components/layout/TopNavLayot.tsx
export default function TopNavLayot({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <div className="min-h-screen">
      <header className="h-14 flex items-center border-b px-(--card-padding) border-(--header-border-color)">
        <h1 className="tracking-(--gap-sm)">Modern Layout</h1>
      </header>
      <main className="m-4">{children}</main>
    </div>
  );
}
```

```tsx
import type { ComponentType, ReactNode } from 'react';

import dynamic from 'next/dynamic';

import type { Layout } from '@/configs/brands/types';

export const AppLayouts: Record<Layout, ComponentType<{ children: ReactNode }>> = {
  'top-nav': dynamic(() => import('./top-nav'), { ssr: true }),
  'side-nav': dynamic(() => import('./side-nav'), { ssr: true }),
};
```

## 服务端：获取品牌配置

> `src/lib/brand.ts`

```ts
// src/libs/brand.ts
import { cookies } from 'next/headers';

import brandConfig from '@/configs/brands';
import type { BrandConfig, Layout, Skin, Theme } from '@/configs/brands/types';

/**
 * SSR 读取品牌配置
 * 优先级：cookie > 配置文件
 */
export async function getBrandConfigSSR(): Promise<BrandConfig> {
  // 1. 读取 cookie
  const cookieStore = await cookies();
  const theme = cookieStore.get('theme')?.value as Theme | undefined;
  const skin = cookieStore.get('skin')?.value as Skin | undefined;
  const layout = cookieStore.get('layout')?.value as Layout | undefined;
  // 2. 返回品牌配置
  return {
    ...brandConfig,
    theme: theme || brandConfig.theme,
    skin: skin || brandConfig.skin,
    layout: layout || brandConfig.layout,
  };
}
```

## 客户端：共享主题配置

> `src/providers/brand.provider.ts`

```tsx
// src/providers/brand.provider.tsx

'use client';

import { type ReactNode, createContext, useContext } from 'react';

import type { BrandConfig } from '@/configs/brands/types';

const BrandConfigContext = createContext<BrandConfig | null>(null);

export function BrandConfigProvider({
  value,
  children,
}: {
  value: BrandConfig;
  children: ReactNode;
}) {
  return <BrandConfigContext.Provider value={value}>{children}</BrandConfigContext.Provider>;
}

export function useBrandConfig() {
  const ctx = useContext(BrandConfigContext);
  if (!ctx) throw new Error('必须在 BrandConfigProvider 上下文中使用 useBrandConfig');
  return ctx;
}
```

## 根布局

> `src/app/[lang]/layout.tsx`

```tsx
// src/app/[locale]/layout.tsx
import type { Metadata, Viewport } from 'next';
import { NextIntlClientProvider, hasLocale } from 'next-intl';
import { getMessages, setRequestLocale } from 'next-intl/server';
import { notFound } from 'next/navigation';

import '@/app/globals.css';
import '@/assets/styles/generated/brand.css';
import { routing } from '@/i18n/routing';
import { getBrandConfigSSR } from '@/libs/brand';
import { BrandConfigProvider } from '@/providers/brand.provider';

export const runtime = 'edge';
export const viewport: Viewport = {
  width: 'device-width',
  initialScale: 1,
  minimumScale: 1,
  maximumScale: 1,
  userScalable: false,
  viewportFit: 'cover',
};

export const metadata: Metadata = {
  title: 'Create Next App',
  description: 'Generated by create next app',
};

export default async function LocaleLayout({
  children,
  params,
}: Readonly<{
  children: React.ReactNode;
  params: { locale: string };
}>) {
  const { locale } = await params;

  // Ensure that the incoming `locale` is valid
  if (!hasLocale(routing.locales, locale)) {
    notFound();
  }

  // Enable static rendering
  setRequestLocale(locale);

  // Providing all messages to the client
  // side is the easiest way to get started
  const messages = await getMessages();

  // 获取品牌配置
  const brandConfig = await getBrandConfigSSR();

  return (
    <html lang={locale}>
      <head>
        {/* Theme color - 谷歌浏览器工具栏主题色（safari取页面颜色，一般是body） */}
        <meta name="theme-color" content={'#874334'} />
        {/* PWA - 隐藏地址栏、底部工具栏 */}
        {/* For iOS Safari*/}
        <meta name="apple-mobile-web-app-capable" content="yes" />
        {/* For Android Chrome */}
        <meta name="mobile-web-app-capable" content="yes" />
      </head>
      <body style={{ background: '#F5F5F5' }}>
        <NextIntlClientProvider messages={messages} locale={locale}>
          <BrandConfigProvider value={brandConfig}>
              {children}
              ...
          </BrandConfigProvider>
        </NextIntlClientProvider>
      </body>
    </html>
  );
}

```

## 业务组件

展示 token/skin 的使用

> `src/app/[lang]/_components/ClientInfo.tsx`

```tsx
// src/app/[lang]/_components/ClientInfo.tsx
"use client";
import { useBrandConfig } from "@/providers/brand.provider";

export default function ClientInfo() {
  const brand = useBrandConfig();
  return (
    <div>
      <h2 className="mb-4">—— 客户端组件 ——</h2>
      <h2>当前主题: {brand.theme}</h2>
      <h2>当前皮肤: {brand.skin}</h2>
      <h2>当前布局: {brand.layout}</h2>
      <h2>当前品牌: {brand.brandName}</h2>
    </div>
  );
}
```

> `src/app/[lang]/_components/ServerInfo.tsx`

```tsx
// src/app/[lang]/_components/ServerInfo.tsx

import { getBrandConfigSSR } from "@/lib/brand";

export default async function ServerInfo() {
  const brand = await getBrandConfigSSR();
  return (
    <div>
      <h2 className="mb-4">—— 服务端组件 ——</h2>
      <h2>当前主题: {brand.theme}</h2>
      <h2>当前皮肤: {brand.skin}</h2>
      <h2>当前布局: {brand.layout}</h2>
      <h2>当前品牌：{brand.brandName}</h2>
    </div>
  );
}
```

```tsx
// src/app/[lang]/_components/Card.tsx
import Image from "next/image";
import { getBrandConfigSSR } from "@/libs/brand";
import { getImgUrl } from "@/libs/utils";
import ClientInfo from "./ClientInfo";
import ServerInfo from "./ServerInfo";

export default async function Card() {
  const brand = await getBrandConfigSSR();
  return (
    <div className="flex gap-4 border border-(--card-border-color)  rounded-(--card-radius) p-(--card-padding) bg-(--card-bg) text-(--card-text)">
      <Image
        width={300}
        height={200}
        src={getImgUrl(brand, "banner.jpg")}
        alt="banner"
        priority
      />
      <div className="space-y-4">
        <p>Example card. Colors and radius come from theme/skin tokens.</p>
        <button
          type="button"
          className="px-4 py-2 inline-block text-sm bg-(--color-primary) text-white"
        >
          Primary
        </button>
        <div className="flex items-center gap-4">
          <ClientInfo />
          <div className="h-10 w-px bg-gray-400 mx-4"></div>
          <ServerInfo />
        </div>
      </div>
    </div>
  );
}
```

## 页面调用

```tsx
// src/app/[lang]/page.tsx
import ThemeSkinSwitcher from "@/components/features/ThemeSkinSwitcher";
import ClientInfo from "./_components/ClientInfo";
import ServerInfo from "./_components/ServerInfo";
import ServerInfo from "./_components/Card";

export default async function Page() {
  return (
    <div>
      <ThemeSkinSwitcher />
      <Card />
    </div>
  );
}
```

# 扩展 · 主题皮肤切换

在 Next.js（App Router）中实现主题与皮肤切换时，为保证 **服务端组件（RSC）与客户端组件（Client Component）之间的表现一致性**，通常需要在用户切换主题 / 皮肤后，将选择结果持久化（如写入 Cookie），并通过 **页面刷新** 的方式触发服务端重新渲染。

基于这一前提，现有实现需要在以下几个方面进行调整与补充。

## 1️⃣ 样式加载策略调整

由于 Next.js 对 **运行时动态导入 CSS** 存在限制，当前方案采用在 **构建阶段生成 brand.css**，并在 layout 中直接引入的方式加载样式。

为满足 **运行时切换主题 / 皮肤** 的需求，同时避免影响 **LCP 等性能指标**，主题与皮肤相关的样式不再拆分为多个 CSS 文件，而是将所有视觉 Tokens 统一收敛到一个样式文件中：

```
@/assets/styles/brand.css
```

layout 中的样式引入方式保持不变，通过 **变量或选择器切换** 实现不同主题 / 皮肤的表现，从而避免运行时加载额外样式资源。

## 2️⃣ 服务端品牌配置读取逻辑调整

在服务端渲染阶段，品牌配置的读取需要同时考虑 **默认配置** 与 **用户偏好**。

具体策略如下：

- 优先根据环境变量读取当前品牌的基础配置。
- 再从 Cookie 中读取用户当前选择的主题 / 皮肤。
- 若 Cookie 中不存在对应值，则回退至品牌默认配置。

```ts
// src/libs/brand.ts

import { cookies } from "next/headers";
import type { BrandConfig, Layout, Skin, Theme } from "@/configs/brands/types";

/**
 * SSR 读取包网信息
 * 优先级：cookie > 环境变量 > 默认包网
 */
export async function getBrandConfigSSR(): Promise<BrandConfig> {
  // 1. 根据环境变量，读取品牌配置
  const brandName = process.env.NEXT_PUBLIC_BRAND_NAME;
  const module = await import(`@/configs/brands/${brandName}.ts`);
  const brandConfig = module.default as BrandConfig;
  // 2. 根据 cookie 读取品牌配置
  const cookieStore = await cookies();
  const theme = cookieStore.get("theme")?.value as Theme | undefined;
  const skin = cookieStore.get("skin")?.value as Skin | undefined;
  const layout = cookieStore.get("layout")?.value as Layout | undefined;
  // 3. 返回包网配置
  return {
    ...brandConfig,
    theme: theme ?? brandConfig.theme,
    skin: skin ?? brandConfig.skin,
    layout: layout ?? brandConfig.layout,
  };
}
```

该逻辑确保：

- **首次访问** 使用品牌默认主题 / 皮肤。
- **用户切换后刷新页面**，服务端即可感知并返回一致的渲染结果。
- 避免 SSR / CSR 样式不一致问题。

## 3️⃣ 客户端主题 / 皮肤切换能力封装

在客户端侧，通过封装 `useThemeActions` Hook，统一管理主题与皮肤的切换行为。

切换逻辑主要包括两步：

1. 将用户选择写入 Cookie，用于服务端识别；
2. 主动刷新页面，触发服务端重新渲染。

```tsx
// src/hooks/useThemeActions.ts
import type { Layout, Skin, Theme } from '@/configs/brands/types';
import cookieHelper from '@/libs/cookie-helper';

export function useThemeActions() {
  const setSkin = (newSkin: Skin) => {
    cookieHelper.set('skin', newSkin, { path: '/', expires: 365 });
    window.location.reload();
  };
  const setTheme = (newTheme: Theme) => {
    cookieHelper.set('theme', newTheme, { path: '/', expires: 365 });
    window.location.reload();
  };

  const setLayout = (newLayout: Layout) => {
    cookieHelper.set('layout', newLayout, { path: '/', expires: 365 });
    window.location.reload();
  };
  return { setSkin, setTheme, setLayout };
}
```

该方式虽然引入了刷新行为，但换来的是：

- RSC 与 Client Component 的完全一致性；
- 更简单、可控的状态来源；
- 更符合 Next.js 当前的推荐使用模型。

> 提示：`cookieHelper` 是一个工具类，可选择 `js-cookie` 三方库。

## 4️⃣ 主题 / 皮肤切换组件

在此基础上，提供一个纯客户端的 ThemeSkinSwitcher 组件，用于演示主题与皮肤的切换效果。

组件职责保持单一：

- 读取当前品牌上下文中的主题 / 皮肤状态；
- 触发对应的切换动作；
- 不直接关心样式实现细节。

```tsx
// src/components/features/ThemeSkinSwitcher.tsx
"use client";

import clsx from "clsx";
import { useThemeActions } from "@/hooks/useThemeActions";
import { useBrandConfig } from "@/providers/brand.provider";

export default function ThemeSkinSwitcher() {
  const { setTheme, setSkin, setLayout } = useThemeActions();
  const { theme, skin, layout } = useBrandConfig();
  return (
    <div className="mb-4">
      <div className="flex items-center gap-4">
        <span>主题切换：</span>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": theme === "modern",
          })}
          onClick={() => setTheme("modern")}
        >
          Modern
        </button>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": theme === "classic",
          })}
          onClick={() => setTheme("classic")}
        >
          Classic
        </button>
      </div>
      <div className="flex items-center gap-4">
        <span>皮肤切换：</span>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": skin === "light",
          })}
          onClick={() => setSkin("light")}
        >
          Light
        </button>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": skin === "dark",
          })}
          onClick={() => setSkin("dark")}
        >
          Dark
        </button>
      </div>
      <div className="flex items-center gap-4">
        <span>布局切换：</span>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": layout === "top-nav",
          })}
          onClick={() => setLayout("top-nav")}
        >
          top-nav
        </button>
        <button
          className={clsx("cursor-pointer", {
            "text-(--color-primary)": layout === "side-nav",
          })}
          onClick={() => setLayout("side-nav")}
        >
          side-nav
        </button>
      </div>
    </div>
  );
}
```

## 5️⃣ 移除预构建指令

在引入统一的样式加载与主题 / 皮肤切换方案后，样式资源已不再依赖运行时或构建前的动态生成逻辑，因此可以安全移除 `package.json` 中的 `prebuild` 与 `predev` 脚本指令。

# 研发维护指南（面向开发）

**新增主题**

1. 在 public/styles/themes 目录按现有格式创建新主题文件
2. 在 src/configs/brands/types.ts 为 Theme 添加新类型

**新增皮肤**

1. 在 public/styles/skins 目录按现有格式创建新皮肤文件。
2. 在 src/configs/brands/types.ts 为 Skin 添加新类型。

**新增品牌**

1. 在 src/configs/brands 目录按品牌名称创建配置文件。
2. 在 src/configs/brands/types.ts 为 BrandId 添加新类型。
3. 为新品牌创建环境变量文件，并指定 NEXT_PUBLIC_BRAND_ID。

注意事项：

1. **遵循命名规范**：文件和目录保持与现有格式一致，避免随意命名，保证资源能正确加载。
2. **品牌标识完整**：确保所有环境下的环境变量、配置文件和构建脚本中品牌 ID 一致，保证 SSR 与 CSR 都能正确读取。
3. **类型安全**：在 brands/types.ts 补齐类型，利用 TypeScript 校验防止配置错误。
4. **保持一致性**：文件命名、目录层级和覆盖文件（overrides）统一，避免资源查找失败或重复冗余。
