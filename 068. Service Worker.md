# 概述

[Service Worker ↪](https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API)（服务工作线程）是浏览器中的一种脚本，运行在 **独立于主线程** 的 Worker 中，位于网页与网络之间，充当“中间人”。它 **不依赖页面生命周期**，页面关闭后仍可存在，主要用于：

1. **离线缓存**：拦截网络请求、缓存资源，使页面在无网或弱网时仍可访问
2. **网络代理**：拦截、修改、重定向请求，实现自定义缓存策略
3. **后台能力**：推送通知（Push）、后台同步（Background Sync）、周期性后台同步（Periodic Background Sync）等

**特点**：独立线程、事件驱动、仅支持 **HTTPS 或 localhost**。本文内容基于 **2025–2026 年** 的规范与主流浏览器实现（W3C Service Workers 为 Candidate Recommendation Draft，[规范 Nightly](https://w3c.github.io/ServiceWorker/) 持续演进）。

---

# 前置概念

## Web Worker vs. Service Worker

| **对比项**       | **Web Worker**               | **Service Worker**                     |
| ---------------- | ---------------------------- | -------------------------------------- |
| 主要作用         | 耗时计算，避免阻塞主线程     | 拦截请求、缓存、离线、后台能力         |
| 是否拦截网络请求 | ❌ 不可以                     | ✅ 可以（fetch 事件）                   |
| 生命周期         | 依附于页面，页面关闭即结束   | 独立于页面，可长期存在                 |
| 与页面通信       | postMessage                  | postMessage、事件                      |
| 访问 DOM         | ❌ 不可以                     | ❌ 不可以                               |
| 使用场景         | 大量计算、编解码、数据处理   | 离线缓存、PWA、推送、后台同步          |
| 运行环境         | HTTP/HTTPS 均可               | 仅 HTTPS 或 localhost                  |

## 重要特性与限制

- **不能访问 DOM**：运行在 Worker 上下文中，无法操作 `document`、`window`。
- **不能使用同步 API**：设计为完全异步，`XMLHttpRequest`、`localStorage` 等同步接口不可用，需用 `fetch`、Cache API、IndexedDB 等异步方式。
- **必须 HTTPS 或 localhost**：出于安全考虑，修改网络请求的能力只能在安全上下文中使用；本地开发可用 `http://localhost`。
- **大量使用 Promise**：安装、缓存、fetch 等均为异步，`event.waitUntil(promise)` 用于延长生命周期直到异步完成。
- **生命周期独立于页面**：页面关闭后 SW 仍可存在；更新时旧 SW 可继续控制已打开页面，新 SW 进入“等待”直到无页面使用旧版。
- **模块化**（2025+）：以 `type: 'module'` 注册时，SW 内可使用**静态** `import ... from '...'`，与主应用共享模块；**不支持**动态 `import()`，且需浏览器支持。

## 兼容性

- **核心 API**：Chrome、Firefox、Edge、Safari 11.1+ 均已支持，可查 [Can I use - Service Workers](https://caniuse.com/serviceworkers)。
- **ES Module 形态**：`register(scriptURL, { type: 'module' })` 在 Chrome 91+、Safari 15+、Firefox 114+ 等支持，SW 内仅支持**静态** `import`，不支持动态 `import()`。
- **静态路由（Static Routing API）**：`InstallEvent.addRoutes()` 在 **Chrome 123+** 可用，可提前声明部分请求直走网络/缓存、不经 SW，减少首屏等待，其他浏览器尚在跟进。
- **后台同步**：一次性同步（`SyncManager`）在 Chrome/Edge 支持较好，Safari/Firefox 支持有限；周期性同步（`PeriodicSyncManager`）需 PWA 安装且目前多为 Chromium 系支持。

---

# 从 0 到 1：Service Worker 生命周期

## 1. 注册（Registration）

页面通过 `navigator.serviceWorker.register(scriptURL, options)` 注册 Service Worker：

- **scriptURL**：SW 脚本的 URL（通常放在项目根或 `public` 下）
- **options.scope**：控制 SW 可控制的页面范围，默认取脚本所在目录
- **options.type**（2025+ 常用）：设为 `'module'` 时，SW 以 **ES Module** 形式加载，可在脚本内使用静态 `import ... from '...'`，便于复用与主应用共享的模块；**不支持** 动态 `import()`，且需浏览器支持（Chrome 91+、Safari 15+、Firefox 114+ 等）

```javascript
// 经典脚本（默认）
navigator.serviceWorker.register('/sw.js', { scope: '/' });

// ES Module 形态（现代写法）
navigator.serviceWorker.register('/sw.js', { scope: '/', type: 'module' });
```

注册成功后，浏览器会**下载并解析** SW 脚本，进入安装阶段。

## 2. 安装（Install）

- 首次注册或脚本内容变化时触发 `install` 事件
- 在 `install` 中通常做：**预缓存关键资源**（HTML、CSS、JS）
- 使用 `event.waitUntil(cache.addAll([...]))` 可延长安装阶段，直到缓存完成
- 若 `waitUntil` 里 Promise reject 或未调用，安装失败，SW 不会进入激活

**静态路由（Static Routing API，Chrome 123+）**：在 `install` 中可调用 `event.addRoutes(routes)`，**提前声明**部分请求不经过 SW 的 fetch 逻辑，直接走网络或缓存，从而减少 SW 启动对首屏的延迟。每条规则可指定 `urlPattern`、`requestMethod`、`requestDestination` 等条件，以及来源 `"network"` / `"cache"` / `"fetch-event"`。详见 [Service Worker Static Routing - Chrome Developers](https://developer.chrome.com/blog/service-worker-static-routing)。

## 3. 激活（Activate）

- 安装完成后进入“等待激活”状态
- 当**没有其他正在控制的页面**使用旧版 SW 时，新 SW 会触发 `activate`
- 在 `activate` 中通常做：**清理旧版本缓存**（按缓存名或版本号删除）

## 4. 控制页面与 fetch

- 激活后，SW 开始**控制**该 scope 下的页面
- 这些页面的**网络请求**会触发 SW 的 `fetch` 事件
- 在 `fetch` 中可：返回缓存、转发到网络、或“网络优先/缓存优先”等策略

## 5. 更新

- 浏览器会定期检查 SW 脚本是否有更新（如 24 小时内至少一次）
- 若脚本字节不同，会安装新 SW，旧 SW 仍控制当前页面，新 SW 进入“等待激活”
- 关闭所有受控页面后，新 SW 激活；也可在代码里调用 `registration.update()` 主动检查更新

**首次加载 vs 更新加载**：首次访问时流程为「注册 → 安装 → 激活 → 控制页面」；更新时为新 SW「安装 → 等待(waiting)」，待旧 SW 控制的页面全部关闭后新 SW 才激活。

**skipWaiting 与 clients.claim**：在 `install` 中调用 `self.skipWaiting()` 可使新 SW 安装完成后立即进入激活，不等待旧 SW 退出；在 `activate` 中调用 `self.clients.claim()` 可让新 SW 立即控制当前已打开的、同 scope 的页面（否则需刷新后才受控）。

---

# 从 0 到 1：手写一个最小可用 SW

下面是一个**最小可用**的 Service Worker：只做“安装时预缓存 + fetch 时优先返回缓存”。

```javascript
// public/sw.js（放在站点可访问的路径，如 public/sw.js）

const CACHE_NAME = 'my-app-v1';

// 安装：预缓存关键资源
self.addEventListener('install', (event) => {
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      return cache.addAll([
        '/',
        '/index.html',
        '/styles/main.css',
        '/scripts/main.js',
      ]);
    })
  );
  self.skipWaiting(); // 可选：安装完立即激活，不等旧 SW 退出
});

// 激活：清理旧缓存
self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names
          .filter((name) => name !== CACHE_NAME)
          .map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim(); // 可选：立即控制所有同 scope 的页面
});

// 请求：缓存优先，没有则走网络
self.addEventListener('fetch', (event) => {
  event.respondWith(
    caches.match(event.request).then((cached) => {
      return cached || fetch(event.request);
    })
  );
});
```

**页面侧注册：**

```javascript
// 在页面或入口脚本中（经典脚本）
if ('serviceWorker' in navigator) {
  window.addEventListener('load', () => {
    navigator.serviceWorker
      .register('/sw.js', { scope: '/' })
      .then((reg) => console.log('SW 注册成功', reg.scope))
      .catch((err) => console.error('SW 注册失败', err));
  });
}

// 若 sw.js 使用 ES Module（import/export），改为：
// .register('/sw.js', { scope: '/', type: 'module' })
```

把 `sw.js` 放到站点根路径（如 `public/sw.js`），保证通过 `https://你的域名/sw.js` 或 `http://localhost:3000/sw.js` 能访问即可。

---

# 常见缓存策略（简要）

- **缓存优先**：先查缓存，有则返回，无则请求网络（适合静态资源）
- **网络优先**：先请求网络，失败再用缓存（适合需要新鲜度的接口）
- **Stale-While-Revalidate**：先返回缓存（若有），同时后台请求网络并更新缓存（适合可接受略旧的页面/接口）

实际项目中可根据资源类型（HTML / API / 静态资源）组合使用不同策略。示例：**网络优先**（接口/HTML）可写为 `fetch(event.request).catch(() => caches.match(event.request))`；**Stale-While-Revalidate** 可先 `caches.match(event.request)` 返回旧数据，再 `fetch` 成功后 `cache.put(request, response)` 更新缓存。

---

# 页面与 Service Worker 通信

SW 与页面通过 **postMessage** 双向通信。

**页面 → SW**：注册完成后用 `navigator.serviceWorker.controller.postMessage('hello')` 发送（同 scope 下 `controller` 为当前控制的 SW）；若 scope 非当前页，可用 `registration.active.postMessage('hello')`（需等 SW 激活，可用 `navigator.serviceWorker.ready` 或轮询 `reg.active`）。

**SW → 页面**：在 SW 内通过 `event.source.postMessage('from sw')` 回发给触发消息的页面；或 `self.clients.matchAll()` 拿到所有受控窗口再对每个 `client.postMessage()` 广播。

**页面侧接收**：`navigator.serviceWorker.addEventListener('message', (e) => console.log(e.data))`。

```javascript
// 页面：注册后发送
navigator.serviceWorker.register('/sw.js').then((reg) => {
  if (navigator.serviceWorker.controller) {
    navigator.serviceWorker.controller.postMessage({ type: 'PING' });
  }
  navigator.serviceWorker.addEventListener('message', (e) => {
    if (e.data?.type === 'PONG') console.log('SW 已响应');
  });
});

// sw.js：接收并回复
self.addEventListener('message', (event) => {
  if (event.data?.type === 'PING') {
    event.source.postMessage({ type: 'PONG' });
  }
});
```

---

# 后台同步（Background Sync）

当用户提交数据时若网络不可用，普通页面会随关闭而中断请求。利用 SW **在页面关闭后仍可运行**，可先在前端登记“待同步任务”，等网络恢复后由 SW 在后台执行。

**一次性同步（One-Off）**：页面在 SW 就绪后调用 `registration.sync.register('data_sync')` 注册一个 tag；SW 监听 **`sync`** 事件，在回调里根据 `event.tag` 执行逻辑（如重试未成功的请求）。接口：`SyncManager.register(tag)`、`getTags()`；支持度：Chrome/Edge 良好，Safari/Firefox 有限，使用前需 `if ('sync' in registration)` 能力检测。

**周期性后台同步（Periodic Background Sync，2025+）**：[Web Periodic Background Synchronization API](https://developer.mozilla.org/en-US/docs/Web/API/Web_Periodic_Background_Synchronization_API) 允许在 SW 内**按间隔**拉取新数据（如新闻、天气），即使应用未打开。用法：`registration.periodicSync.register('fetch-news', { minInterval: 24 * 60 * 60 * 1000 })`；SW 监听 **`periodicsync`** 事件。注意：通常需 **PWA 已安装** 且当前多为 Chromium 系支持，Safari/Firefox 尚未普及，需 `'periodicSync' in registration` 检测。

---

# 调试与更新

- **Chrome DevTools**：Application → Service Workers 可查看当前 SW 状态、Unregister、勾选 **Update on reload**（每次刷新都拉取最新 sw.js，便于开发）。
- **主动更新**：`navigator.serviceWorker.register('/sw.js').then(reg => reg.update())` 可主动检查脚本是否有更新。
- **感知更新并提示用户**：在注册后监听 `reg.addEventListener('updatefound', () => { ... })`，在 `reg.installing` 上监听 `statechange`，当 `state === 'installed'` 且已有 `navigator.serviceWorker.controller` 时说明有新版本在等待，可提示用户“刷新以获取最新内容”。

---

# Workbox 简介（2025–2026）

手写原生 SW 在缓存策略、版本管理、预取等方面容易重复造轮子。[Workbox](https://developer.chrome.com/docs/workbox/) 是 Chrome 团队维护的**生产级** SW 工具库，当前主版本为 **v7**（如 v7.4.0，2024 年 11 月仍在维护），提供：

- **运行时**：`workbox-routing`、`workbox-strategies`（StaleWhileRevalidate、NetworkFirst 等）、`workbox-precaching`、`workbox-expiration`、`workbox-background-sync` 等
- **构建集成**：CLI、Node 模块、Webpack/Vite 等插件，用于生成 SW 和预缓存列表
- **workbox-window**：在页面侧处理注册、更新检测、生命周期，便于与框架（如 React）配合

原生 Cache Storage 无内置过期策略，Workbox 可自动管理缓存版本与过期，并带日志与错误上报。复杂 PWA 或需要细粒度策略时，建议直接采用 Workbox 而非手写完整 `sw.js`。

---

# Next.js 16+（App Router）接入示例

在 Next.js 16+ App Router 中，页面和资源由服务端与客户端共同决定，SW 通常只做**客户端缓存与离线**。推荐做法：

1. **SW 脚本放在 `public` 下**，例如 `public/sw.js`，这样访问路径为 `/sw.js`。
2. **在客户端根布局或根组件中注册 SW**，避免在服务端执行 `navigator.serviceWorker`。

## 1. 目录与文件

```
your-next-app/
├── app/
│   ├── layout.tsx
│   └── page.tsx
├── public/
│   └── sw.js          # Service Worker 脚本
└── components/
    └── SwRegistration.tsx   # 客户端注册组件
```

## 2. 客户端注册组件

```tsx
// components/SwRegistration.tsx
'use client';

import { useEffect } from 'react';

export function SwRegistration() {
  useEffect(() => {
    if (typeof window === 'undefined' || !('serviceWorker' in navigator)) return;

    navigator.serviceWorker
      .register('/sw.js', { scope: '/' })
      .then((reg) => {
        console.log('Service Worker 注册成功', reg.scope);
        // 可选：检查更新
        reg.addEventListener('updatefound', () => {
          const newWorker = reg.installing;
          if (!newWorker) return;
          newWorker.addEventListener('statechange', () => {
            if (newWorker.state === 'installed' && navigator.serviceWorker.controller) {
              console.log('有新版本，可提示用户刷新');
            }
          });
        });
      })
      .catch((err) => console.error('Service Worker 注册失败', err));
  }, []);

  return null;
}
```

## 3. 在根布局中挂载

```tsx
// app/layout.tsx
import { SwRegistration } from '@/components/SwRegistration';

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html lang="zh-CN">
      <body>
        {children}
        <SwRegistration />
      </body>
    </html>
  );
}
```

## 4. `public/sw.js` 示例（App Router 友好）

App Router 下路由是动态的，SW 通常只缓存**明确已知**的静态资源或做**按需缓存**，不做“预缓存所有页面”：

```javascript
// public/sw.js
const CACHE_NAME = 'next-app-v1';

self.addEventListener('install', (event) => {
  self.skipWaiting();
  event.waitUntil(
    caches.open(CACHE_NAME).then((cache) => {
      // 只预缓存明确需要的静态资源
      return cache.addAll(['/']);
    })
  );
});

self.addEventListener('activate', (event) => {
  event.waitUntil(
    caches.keys().then((names) => {
      return Promise.all(
        names.filter((name) => name !== CACHE_NAME).map((name) => caches.delete(name))
      );
    })
  );
  self.clients.claim();
});

self.addEventListener('fetch', (event) => {
  // 仅对同源 GET 请求做缓存
  if (event.request.method !== 'GET' || !event.request.url.startsWith(self.location.origin)) {
    return;
  }
  event.respondWith(
    caches.match(event.request).then((cached) => {
      return cached || fetch(event.request).then((res) => {
        const clone = res.clone();
        caches.open(CACHE_NAME).then((cache) => cache.put(event.request, clone));
        return res;
      });
    })
  );
});
```

生产环境建议对 HTML 使用**网络优先**或 **Stale-While-Revalidate**，对静态资源（如 `_next/static`）使用**缓存优先**，并按需调整 `CACHE_NAME` 和缓存列表。

---

# 小结

- Service Worker 是**独立于页面的网络中间层**，用于离线、缓存和后台能力；受限于 Worker 环境（无 DOM、仅异步 API、HTTPS/localhost）。规范处于 W3C Candidate Recommendation Draft（2026），持续演进。
- 生命周期：**注册 → 安装 → 激活 → 控制页面并处理 fetch**；更新时新 SW 先安装、进入 waiting，待旧 SW 控制的页面关闭后才激活；可用 `skipWaiting` / `clients.claim` 改变这一行为。
- **2025–2026 常用能力**：注册时可传 `type: 'module'` 使用 ES Module 形态（仅静态 import）；Chrome 123+ 可用 `InstallEvent.addRoutes()` 做静态路由、减少首屏延迟；后台同步区分一次性（sync）与周期性（periodicsync，多需 PWA）。
- 从 0 到 1：先写“安装预缓存 + fetch 缓存优先”的 `sw.js`，再在页面里 `register('/sw.js')`；缓存策略按资源类型区分（静态缓存优先、接口网络优先或 Stale-While-Revalidate）。
- 页面与 SW 通过 **postMessage** 双向通信；复杂项目可选用 **Workbox v7** 做策略与构建集成。
- 在 Next.js 16+ App Router 中：`sw.js` 放 `public`，用客户端组件在根布局注册；避免过度预缓存动态路由。

---

# 参考

[1]. [Service Worker API - MDN ↪](https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API)  
[2]. [Workbox - Chrome for Developers ↪](https://developer.chrome.com/docs/workbox)  
[3]. [Service Worker Static Routing - Chrome Developers ↪](https://developer.chrome.com/blog/service-worker-static-routing)  
[4]. [ES modules in service workers - web.dev ↪](https://web.dev/articles/es-modules-in-sw)  
[5]. [Web Periodic Background Synchronization API - MDN ↪](https://developer.mozilla.org/en-US/docs/Web/API/Web_Periodic_Background_Synchronization_API)  
[6]. [Service Workers W3C Nightly ↪](https://w3c.github.io/ServiceWorker/)  
[7]. [一文搞懂前端 service-worker 技术 - lmcc ↪](https://www.lmcc.top/articles/73.html)
