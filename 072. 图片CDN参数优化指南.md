# 概述

本文档用于规范 **通过 URL 参数对图片进行动态优化** 的使用方式，适用于 **支持链接参数处理的图片 CDN / 图片服务**（如 Cloudflare Images/CDN、Imgix、Cloudinary、Fastly 等）。

目标：

- ☑️ 在不明显降低视觉质量的前提下，**最大化减少图片体积**
- ☑️  适配不同设备分辨率（普通屏 / Retina / 高分屏）
- ☑️ 形成一套 **CDN 无关、可迁移的图片优化使用习惯**

> ⚠️ 注意：并非所有 CDN 参数完全一致，本文以 [Cloudflare Images/CDN ↪](https://developers.cloudflare.com/images/transform-images/) 为示例，参数命名需以具体 CDN 文档为准，但优化思路通用。

# CDN图片URL构成

```
https://<ZONE>/cdn-cgi/image/<OPTIONS>/<SOURCE-IMAGE>
```

- `<ZONE>`：图片域名
- `/cdn-cgi/image/`：固定前缀，表示使用 CDN 图像处理服务
- `<OPTIONS>`：图像处理选项，用于指定格式、质量、尺寸等参数
- `<SOURCE-IMAGE>`：源图像路径，通常包括套图名称和具体图片文件

> 建议将此格式作为封装函数或组件的基础输入，确保 CDN URL 构建统一。

# 核心参数（重点）

| 参数      | 作用       | 推荐使用方式                                                 |
| --------- | ---------- | ------------------------------------------------------------ |
| `format`  | 图片格式   | 默认 `auto`，自动选择 WebP/AVIF，可简写 `f=auto`             |
| `q`       | 图片质量   | 70–80（默认 75），安卓设备可适当降低到 60，可简写 `q=60`     |
| `dpr`     | 屏幕像素比 | 1 / 1.5 / 2（建议 ≤2）                                       |
| `w` / `h` | 输出尺寸   | 强烈建议指定，可简写 `w=100` / `h=100`                       |
| `fit`     | 缩放裁剪   | `cover` / `contain` / `scale-down`                           |
| 其他参数  | 高级效果   | `blur`、`anim`、`background`、`brightness`、`flip`、`rotate`、`trim` 等。 |

> 提示：优先使用 format / width / height / quality / dpr 来保证图片质量，其它参数可根据场景增强 UX 或优化性能。

# 常用优化组合

1️⃣ 普通网页/列表图（性能优先）

```
?format=auto&q=75&dpr=1&w=300
```

- 默认首选方案
- 体积小、加载快

2️⃣ 高分屏 / Retina 设备

```
?format=auto&q=80&dpr=2&w=300
```

- 视觉更清晰
- 不建议 `dpr > 2`

3️⃣ 固定比例封面/卡片图

```
?format=webp&q=80&w=400&h=300&fit=cover
```

- 保持比例并裁剪
- 适合 Banner / 封面 / 卡片

# 使用建议

1. 默认使用 `format=auto` 让 CDN 选择最佳格式。

2. 质量 `q` 控制在 70–80，90+ 基本没有性价比

3. 限制 dpr ≤ 2，3 会明显增大体积

4. 一定要指定 w / h，避免返回原图

5. 根据场景选择 fit：

   - `cover`：填满容器（可能裁剪）
   - `contain`：完整显示（可能留白）

# 响应式及设备适配

- 对于 Banner / 大图，建议使用响应式处理，利用 `srcset` 属性。

- 需要区分 Android / iOS / Desktop：
  - 服务端：通过 `header('User-Agent')` 判断
  - 客户端：通过 `navigator.userAgent` 判断
- CDN 缓存策略需与设备判断逻辑一致。

# 通用迁移原则（跨CDN）

即使未来更换 CDN，也请遵循以下通用原则：

- 📏 **尺寸优先**：永远按展示尺寸请求图片
- 🎯 **质量分级**：列表图 < 正文图 < 视觉主图
- 📱 **DPR 有上限**：清晰 ≠ 盲目更大
- 🔄 **参数可替换，思路不变**

> 示例：`format/q/dpr/w/h` 在不同 CDN 中可能叫 `fm/quality/dpr/width/height`，但含义一致。
