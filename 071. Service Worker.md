# 概述

[Service Worker ↪](https://developer.mozilla.org/zh-CN/docs/Web/API/Service_Worker_API)（服务工作线程）是浏览器中的一种脚本，运行在网页和网络之间的独立线程中，充当浏览器与服务器之间的 “中间人”，它不依赖于页面生命周期。它主要用于实现以下功能：

1. **离线缓存**：可以拦截网络请求，缓存资源，使网页在无网络或网络不稳定时仍能工作。
2. 后台任务：能在后台处理推送通知（Push Notification）、同步数据等任务，即使用户关闭了网页。
3. 网络代理：可以拦截、修改、重定向网络请求，实现自定义的请求策略。

特点：

- 独立线程：不会阻塞主线程，运行在浏览器后台。
- 事件驱动：主要通过 install、activate、fetch 等事件来响应操作。
- 安全限制：只能在 HTTPS 或 localhost 下使用，保证安全。

## Web Worker vs. Service Worker 

Web Worker 解决的是 **性能问题（不阻塞页面）**，而 Service Worker 解决的是 **网络与离线能力问题**。

| **对比项**       | **Web Worker**               | **Service Worker**                     |
| ---------------- | ---------------------------- | -------------------------------------- |
| 主要作用         | 处理耗时计算，避免阻塞主线程 | 拦截请求、缓存资源、离线能力、后台功能 |
| 是否拦截网络请求 | ❌ 不可以                     | ✅ 可以（fetch 事件）                   |
| 生命周期         | 依附于页面，页面关闭即结束   | 独立于页面，页面关闭仍可能存在         |
| 与页面通信       | 通过 postMessage             | 通过 postMessage、事件机制             |
| 访问 DOM         | ❌ 不可以                     | ❌ 不可以                               |
| 使用场景         | 大量计算、数据处理、解码等   | 离线缓存、PWA、推送通知、后台同步      |
| 运行环境         | 任意环境（HTTP/HTTPS）       | 仅 HTTPS 或 localhost                  |

# 参考

[1]. [一文搞懂前端service-worker 技术 ↪](https://www.lmcc.top/articles/73.html)

