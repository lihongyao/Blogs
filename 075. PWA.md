# 概述

[PWA](https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps)（Progressive Web App，渐进式 Web 应用）是一类**用 Web 技术构建、具备“类原生”体验**的 Web 应用。通过 **Web App Manifest** 和 **Service Worker** 等标准能力，使网站在可靠、可安装、可扩展三方面接近原生 App，同时保持“打开即用、无需应用商店”的 Web 特性。

**PWA 的三大支柱：**

1. **可靠（Reliable）**：在弱网或离线时仍能打开并展示内容，依赖 Service Worker 做缓存与离线策略。
2. **可安装（Installable）**：可被用户“安装”到主屏/桌面，以独立窗口启动，具备应用名、图标、启动页等，依赖 **Web App Manifest** 和 HTTPS。
3. **可扩展（Capable）**：可接入推送通知、后台同步、快捷方式等能力，进一步接近原生体验。

**特点**：基于现有 Web 技术、渐进增强（不支持的环境仍可当普通网站用）、需 **HTTPS**，与 [Service Worker](068.%20Service%20Worker.md) 强绑定。本文在 2025–2026 年规范与主流浏览器实现基础上，从 0 到 1 说明如何把一个站点做成可安装、可离线的 PWA。

---

# 前置概念

## PWA vs. 普通网站 vs. 原生 App

| **对比项**   | **普通网站**       | **PWA**                     | **原生 App**       |
| ------------ | ------------------ | --------------------------- | ------------------ |
| 安装         | 无需安装           | 可选安装到主屏/桌面         | 需应用商店下载     |
| 离线         | 一般不可用         | 可离线使用（SW 缓存）       | 视实现而定         |
| 推送         | 无                 | 支持（Push API）            | 支持               |
| 入口         | 浏览器地址栏/书签  | 主屏图标、独立窗口          | 主屏图标           |
| 更新         | 刷新即最新         | 可控（SW + 缓存策略）       | 需发版/商店审核    |
| 技术栈       | HTML/CSS/JS        | HTML/CSS/JS + Manifest + SW | 原生或跨端框架     |

## 可安装的前提（简要）

- **HTTPS**（或 localhost）
- **Web App Manifest**：`name`/`short_name`、`icons`（含 192px、512px）、`start_url`、`display`
- **Service Worker**：已注册且可控制页面（至少 fetch 或安装阶段成功）
- **浏览器启发式**：如 Chrome 会结合用户互动（点击、停留时间等）决定是否展示“安装”入口

---

# 从 0 到 1：最小可安装 PWA 清单

按下面四步即可得到一个**可安装、可离线**的 PWA 骨架：

1. **HTTPS**：站点使用 HTTPS 或本地用 `http://localhost`。
2. **Manifest**：在项目根或 `public` 下放 `manifest.json`（含 `name`、`short_name`、`icons` 192/512、`start_url`、`display: standalone`），在 HTML 中加 `<link rel="manifest" href="/manifest.json" />`。
3. **Service Worker**：提供 `sw.js`（至少实现 `install` 与 `fetch`），在页面加载后调用 `navigator.serviceWorker.register('/sw.js', { scope: '/' })`。具体写法见 [068. Service Worker](068.%20Service%20Worker.md)。
4. **图标**：准备 192×192 和 512×512 的 PNG，路径与 Manifest 中 `icons[].src` 一致。

完成后用 Chrome 打开站点，满足参与度后即可在地址栏或菜单中看到“安装”入口；也可用 `beforeinstallprompt` 做自定义安装按钮。

---

# 从 0 到 1：Web App Manifest

Manifest 是一个 **JSON 配置文件**，告诉浏览器“这是一个可安装的应用”，包括名称、图标、启动地址、显示模式等。

## 1. 在 HTML 中引用

在页面 `<head>` 里通过 `<link rel="manifest">` 指向 manifest 文件：

```html
<link rel="manifest" href="/manifest.json" />
```

## 2. 最小可安装配置

以下字段是 **可安装性** 的常见要求（以 Chrome 等主流为准）：

| **字段**        | **说明** |
| ---------------- | -------- |
| `name`           | 应用全名，用于安装横幅、启动屏等 |
| `short_name`     | 短名，用于主屏图标下方（空间有限时必填） |
| `icons`          | 图标数组，**必须包含 192×192 和 512×512**，`type` 建议 `image/png` |
| `start_url`      | 点击图标后打开的 URL，通常为 `/` 或入口页 |
| `display`        | 显示模式：`standalone` / `fullscreen` / `minimal-ui` / `browser`，**不能为** `browser` 才更容易被当作“可安装应用” |

`prefer_related_applications` 若为 `true` 会倾向推荐原生 App，若希望主推 PWA 安装，应**不写或设为 `false`**。

**示例 `public/manifest.json`：**

```json
{
  "name": "我的 PWA 应用",
  "short_name": "PWA",
  "description": "一款可离线使用的示例应用",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#ffffff",
  "theme_color": "#1976d2",
  "icons": [
    {
      "src": "/icons/icon-192.png",
      "sizes": "192x192",
      "type": "image/png",
      "purpose": "any"
    },
    {
      "src": "/icons/icon-512.png",
      "sizes": "512x512",
      "type": "image/png",
      "purpose": "any"
    }
  ]
}
```

- **theme_color**：影响浏览器 UI（地址栏、状态栏等）颜色。
- **background_color**：启动屏背景色，在首帧渲染前显示。
- **purpose**：`any` 通用，`maskable` 表示图标可被裁剪为圆形/圆角（Android 等），建议至少提供 `any`，有条件可再加 maskable 尺寸。

## 3. 图标尺寸建议

- **必选**：192×192、512×512（满足 Chrome 等“可安装”要求）。
- **推荐**：再提供 144、384 等常见尺寸；若做 `maskable`，按 [Adaptive Icon 规范](https://web.dev/articles/maskable-icon) 留足安全区（如 512 图中内容集中在中间 80%）。

---

# 从 0 到 1：Service Worker 与离线

PWA 的**可靠/离线**能力完全依赖 Service Worker：缓存静态资源、拦截 fetch、在无网时返回缓存。此处只做串联，具体生命周期、缓存策略、更新策略等见 **[068. Service Worker](068.%20Service%20Worker.md)**。

**最小闭环：**

1. 在站点根路径（如 `public/sw.js`）提供 SW 脚本，实现至少：`install`（可预缓存关键页）、`fetch`（如缓存优先或网络优先）。
2. 在页面中注册：`navigator.serviceWorker.register('/sw.js', { scope: '/' })`。
3. 保证站点 **HTTPS**（或 localhost），否则 SW 无法注册。

满足上述后，再配合正确的 Manifest，即可同时满足“可安装”对 SW 的要求。

---

# 从 0 到 1：可安装性与安装提示

## 安装条件（以 Chrome 为例）

除 Manifest + SW + HTTPS 外，浏览器还会参考**用户参与度**，例如：

- 用户与页面有过交互（如点击、触摸）；
- 在页面上停留一定时间（如约 30 秒）；
- 当前未已安装同源 PWA。

满足后，Chrome 可能自动展示地址栏或底部“安装”图标。其他浏览器（Edge、Samsung Internet 等）逻辑类似但细节不同；Safari 在 iOS/macOS 上通过“添加到主屏幕”等方式安装，不一定触发 `beforeinstallprompt`。

## beforeinstallprompt（自定义安装按钮）

在 **Chromium 系**（Chrome、Edge、Samsung Internet 等）中，当 PWA 满足可安装条件时，会触发 **`beforeinstallprompt`**。你可以**阻止默认提示**，把事件对象存下来，在自定义按钮点击时再调用 `prompt()`，从而控制安装出现的时机与样式。

```javascript
let deferredPrompt = null;

window.addEventListener('beforeinstallprompt', (e) => {
  e.preventDefault();
  deferredPrompt = e;
  document.getElementById('install-btn').hidden = false;
});

document.getElementById('install-btn').addEventListener('click', async () => {
  if (!deferredPrompt) return;
  deferredPrompt.prompt();
  const { outcome } = await deferredPrompt.userChoice;
  console.log('用户选择:', outcome); // 'accepted' | 'dismissed'
  deferredPrompt = null;
});
```

- **beforeinstallprompt** 不是标准事件，Safari 等不支持，使用前建议做存在性判断。
- 安装完成后可通过 `window.matchMedia('(display-mode: standalone)')` 或 `navigator.standalone`（部分浏览器）判断是否在“已安装”的独立窗口中运行。

## 检测是否已安装

```javascript
// 独立窗口或“从主屏打开”时，多数 Chromium 会满足其一
const isStandalone = window.matchMedia('(display-mode: standalone)').matches
  || window.navigator.standalone === true;
if (isStandalone) {
  console.log('当前以 PWA 形式运行');
}
```

---

# 图标与启动体验

- **icons**：Manifest 中务必提供 192 与 512，其余尺寸按需增加；`purpose: "maskable"` 可改善在 Android 等上的图标裁剪效果。
- **theme_color** / **background_color**：减少启动时的白屏感，与品牌色一致即可。
- **start_url**：建议指向能离线打开的入口（被 SW 缓存过的页面），否则安装后首次离线打开可能失败。

---

# 可选能力（简要）

- **推送通知**：使用 [Push API](https://developer.mozilla.org/zh-CN/docs/Web/API/Push_API) + Service Worker 的 `push` 事件，需后端推送服务与用户授权。
- **后台同步**：见 [068. Service Worker](068.%20Service%20Worker.md) 中的“后台同步”“周期性后台同步”，适合离线提交、定时拉数据。
- **快捷方式（shortcuts）**：在 Manifest 中配置 `shortcuts` 数组，可在图标长按菜单中展示“打开某页面”等入口。

```json
{
  "shortcuts": [
    {
      "name": "打开设置",
      "short_name": "设置",
      "url": "/settings",
      "icons": [{ "src": "/icons/settings.png", "sizes": "96x96" }]
    }
  ]
}
```

---

# 与框架结合（Next.js / Vite 等）

- **Manifest**：放在静态资源目录（如 `public/manifest.json`），在根布局的 `<head>` 里加 `<link rel="manifest" href="/manifest.json" />`；若用 Next.js，可在 `app/layout.tsx` 或 `pages/_document.js` 中写入。
- **Service Worker**：SW 脚本也放在 `public`（如 `public/sw.js`），在客户端根组件或 layout 的 `useEffect` 中调用 `navigator.serviceWorker.register('/sw.js')`，具体可参考 [068. Service Worker](068.%20Service%20Worker.md) 中的 Next.js 16+ 接入示例。
- **构建期**：可用 [Workbox](https://developer.chrome.com/docs/workbox)（如 workbox-webpack-plugin、vite-plugin-pwa）在构建时生成 SW 和预缓存列表，减少手写 SW 的维护成本。

---

# 小结

- PWA 通过 **Web App Manifest** 和 **Service Worker** 实现**可靠、可安装、可扩展**，在 HTTPS 下用 Web 技术达到接近原生的体验。
- **可安装**需要：Manifest（name、short_name、icons 含 192/512、start_url、display）、已注册的 Service Worker、HTTPS，以及浏览器对“用户参与度”的满足。
- **Manifest** 决定名称、图标、启动地址、显示模式与主题色；**Service Worker** 决定离线与缓存策略，详见 [068. Service Worker](068.%20Service%20Worker.md)。
- 在 Chromium 系中可用 **beforeinstallprompt** 自定义安装时机；通过 **display-mode: standalone** 等可判断是否以 PWA 形式运行。
- 推送、后台同步、快捷方式等为可选增强；与 Next.js/Vite 等结合时，把 manifest 与 sw 放在 public，在客户端注册 SW，必要时用 Workbox 做构建集成。

---

# 参考

[1]. [Progressive Web Apps - MDN ↪](https://developer.mozilla.org/zh-CN/docs/Web/Progressive_web_apps)  
[2]. [What does it take to be installable? - web.dev ↪](https://web.dev/articles/install-criteria)  
[3]. [Web app manifest - web.dev ↪](https://web.dev/learn/pwa/web-app-manifest)  
[4]. [Making PWAs installable - MDN ↪](https://developer.mozilla.org/en-US/docs/Web/Progressive_web_apps/Guides/Making_PWAs_installable)  
[5]. [beforeinstallprompt - MDN ↪](https://developer.mozilla.org/zh-CN/docs/Web/API/Window/beforeinstallprompt_event)  
[6]. [068. Service Worker](068.%20Service%20Worker.md)（本系列）
