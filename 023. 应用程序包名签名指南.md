https://cloud.tencent.com/document/product/243/97789

# Android

## keystore

@See https://ask.dcloud.net.cn/article/35777

### 生成签名证书

1）准备：安装 openjdk

```shell
$ brew install openjdk@17
$ echo 'export PATH="/opt/homebrew/opt/openjdk@17/bin:$PATH"' >> ~/.zshrc
$ source ~/.zshrc
$ keytool -genkey --help
```

2）生成签名语法：

```shell
$ mkdir -p ~/Android/keystores
$ cd ~/Android/keystores
$ keytool -genkey -alias your_alias -keyalg RSA -keysize 2048 -validity 36500 -keystore your_keystore.keystore
```

参数说明：

- `-alias`：证书别名（建议使用英文字母和数字）
- `-keyalg RSA`：使用 RSA 加密算法（2025年仍为行业标准）
- `-keysize 2048`：密钥长度2048位（安全性更高）
- `-validity 36500`：证书有效期，表示 100 年有效期，单位天，建议时间设置长一点，避免证书过期
- `-keystore`：生成的证书文件名（建议.jks或.keystore后缀）

执行命令后会依次提示输入以下信息，这是 Android 签名证书的标准信息：

1. 输入密钥库密码（Keystore Password）

   ```shell
   输入密钥库口令:
   ```

   填写建议：

   - 使用强密码（字母+数字+特殊字符）
   - 长度至少8位
   - **务必牢记此密码！** 以后打包应用都需要

2. 再次输入密钥库密码

   ```shell
   再次输入新口令:
   ```

   重复输入相同的密码进行确认

3. 填写证书信息（按顺序）

   ```shell
   您的名字与姓氏是什么?
     [Unknown]:  Li Hongyao
     
   您的组织单位名称是什么?
     [Unknown]:  Development
     
   您的组织名称是什么?
     [Unknown]:  Org
     
   您所在的城市或区域名称是什么?
     [Unknown]:  ChengDu
     
   您所在的省/市/自治区名称是什么?
     [Unknown]:  SiChuan
     
   该单位的双字母国家/地区代码是什么?
     [Unknown]:  CN
   ```

   填写说明：

   | 字段              | 示例值        | 说明                               |
   | :---------------- | :------------ | :--------------------------------- |
   | **名字与姓氏**    | `Li Hongyao`  | 开发者姓名（可填真实姓名或英文名） |
   | **组织单位名称**  | `Development` | 部门或团队名称                     |
   | **组织名称**      | `Org`         | 公司或组织名称                     |
   | **城市或区域**    | `ChengDu`     | 所在城市                           |
   | **省/市/自治区**  | `SiChuan`     | 省份或直辖市                       |
   | **国家/地区代码** | `CN`          | 中国用 `CN`（必须2字母大写）       |

4. 确认信息是否正确

   ```shell
   CN=Li Hongyao, OU=Development, O=Org, L=ChengDu, ST=SiChuan, C=CN是否正确?
     [否]:  
   ```

   输入 <kbd>y</kbd> 确认信息正确

5. 生成

   ```shell
   正在为以下对象生成 2,048 位RSA密钥对和自签名证书 (SHA256withRSA) (有效期为 3,650 天):
   	 CN=Li Hongyao, OU=Development, O=Org, L=ChengDu, ST=SiChuan, C=CN
   [正在存储 your_keystore.keystore]
   
   ```

### 查看证书信息

可以使用以下命令查看：

```shell
$ keytool -list -v -keystore test.keystore  
Enter keystore password: //输入密码，回车
```

##  Signature

直接通过一个apk，获取安装到手机的第三方应用签名的apk包。 

详情：https://developers.weixin.qq.com/doc/oplatform/Downloads/Android_Resource.html

<img src="./imgs/get_android_sign.jpg" style="zoom:50%;" />

## 获取包名、平台公钥、签名 MD5 值

公钥和 MD5 值可以通过安卓开发工具、Keytool、Jadx-GUI 等多种工具获取，本文以 jadx-gui 为例。

1. 打开终端，运行如下命令安装 jadx。

   ```shell
   $ brew install jadx
   ```

2. 安装完成后，在终端运行如下命令打开 jadx。

   ```shell
   $ jadx-gui
   ```

3. 运行后，单击 **Open file**，打开 APK 包。

   ![](./imgs/jadx-gui.png)

4. 打开 APK 包后，找到并单击 **Resources**，找到并单击 **AndroidManifest.xml**，查看 **APP 包名**。

   ![](./imgs/jadx_package_name.png)

5. 打开 APK 包后，找到并单击 **APK signature**，查看**平台公钥**和**签名 MD5 值**。

   ![](./imgs/jadx_public_key.png)

# iOS

## 证书、标识符、描述文件

1. 登录 [Apple Developer ↪](https://developer.apple.com/cn/)，滑动到底部选择 [证书、标识符和描述文件 (英文) ↪](https://developer.apple.com/account/ios/certificate/)
2. 创建 Certificates
   - 准备 CSR 文件，步骤如下：
     - <kbd>Command</kbd> + <kbd>Space</kbd>，搜索 **钥匙串访问**
     - 打开 **钥匙串访问** 之后，在屏幕左上角点击 **钥匙串访问** — **证书助理** — **从证书颁发机构请求证书”**
   - 点击创建证书，通常需要创建 **iOS App Development**（开发） 和 **iOS Distribution (App Store Connect and Ad Hoc)**（发布） 两种证书。
3. 创建 Identifiers
   - 路径：App IDs — App，然后填写 Description 和 Bundle ID 即可，下面的 Capabilities 根据需要勾选。
4. 创建 Devices
   - 获取 UDID https://www.pgyer.com/tools/udid
5. 创建 Profiles 
   - 开发环境：iOS App Development
   - 发布环境：Ad Hoc & App Store Connect

## 获取公钥

### 获取证书签名文件

苹果开发中，应用公钥存储在**签名证书**（.cer 或 .p12 文件）中

1. 登录 Apple Developer，进入「Certificates, Identifiers & Profiles」→「Certificates」，找到对应的**开发证书（iOS Development）**或**发布证书（iOS Distribution）**，点击「Download」下载 .cer 文件。
2. 打开 Mac「钥匙串访问」，找到已安装的签名证书（名称通常包含“iPhone Developer:”或“iPhone Distribution:”），右键选择「显示简介」。
3. 展开「细节」，即可看到 「公共密钥」，签名即为「指纹」信息。

# HarmonyOS

## 生成证书请求文件（CSR）

1. 打开DevEco Studio，点击顶部菜单栏 **Build > Generate Key and CSR**，进入密钥与CSR生成向导

2. **创建/选择密钥库文件（.p12）**
   - 若未创建过密钥库，点击**New**进入创建页面，填写以下信息：
     - **Key Store File**：设置密钥库存储路径（如`D:\my-release-key.p12`）及文件名（以`.p12`结尾）；
     - **Password**：设置密钥库密码（需包含大写字母、小写字母、数字、特殊符号中的两种以上，长度≥8位，后续签名配置需使用）；
     - **Confirm Password**：再次输入密码确认。
   - 若已有密钥库，点击**Choose Existing**选择现有`.p12`文件，跳过创建步骤
3. **填写密钥信息**
   - **Alias**：密钥别名（用于标识密钥，如`my_app_key`，后续签名需用到）；
   - **Password**：密钥密码（默认与密钥库密码一致，无需手动输入）；
   - **Validity**：证书有效期（建议设置为25年及以上，覆盖应用生命周期）；
   - **Certificate**：填写证书基本信息（如`Common Name`需与别名一致，`Organization`为单位名称，`Country Code`为国家码如`CN`）
4. **生成CSR文件**
   - 进入**Generate CSR**界面，设置CSR文件的**存储路径**（如`D:\`）和**文件名**（如`my_app.csr`），点击**Finish**。生成成功后，指定路径下会得到`.p12`（密钥库）和`.csr`（证书请求）两个文件

## 创建证书

1. 登录 [华为开发平台](https://developer.huawei.com/consumer/cn/)，点击右上角头像 — 立即登录
2. 点击进入 [AppGallery Connect](https://developer.huawei.com/consumer/cn/service/josp/agc/index.html#/) - 「APP」 - 「证书、APP ID和Profile」
3. 左侧菜单进行创建即可

