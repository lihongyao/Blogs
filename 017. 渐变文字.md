# 概述

在前端开发中，让文字拥有渐变色效果可以让页面更具视觉冲击力。本文介绍两种常用方法：**纯 CSS** 和 **TailwindCSS**。

# 纯 CSS 渐变文字

利用 background-clip 和 -webkit-text-fill-color 可以实现渐变文字效果。

```html
<div class="p1">Hello Gradient</div>
```

```css
.gradient-text {
  font-size: 48px;
  font-weight: bold;

  /* 设置渐变背景 */
  background: linear-gradient(90deg, #f3df00, #ff7300);

  /* 文字显示背景 */
  -webkit-background-clip: text;
  -webkit-text-fill-color: transparent;

  /* 兼容其他浏览器 */
  background-clip: text;
  color: transparent;
}
```

原理：文字本身透明，背景渐变显示在文字轮廓上。

# TailwindCSS 渐变文字

如果项目使用 TailwindCSS，可以通过内置类快速实现：

```html
<h1 class="text-6xl font-bold bg-gradient-to-r from-[#f3df00] to-[#ff7300] bg-clip-text text-transparent">
  Hello Gradient
</h1>
```

- bg-gradient-to-r：渐变方向从左到右
- from-orange-500 to-pink-500：渐变颜色
- bg-clip-text text-transparent：让文字显示背景渐变

原理与方法1相同，只是 Tailwind 提供了快捷类，书写更简洁。

# 总结

- **纯 CSS**：灵活，可完全自定义渐变角度和颜色
- **TailwindCSS**：快速、简洁，适合日常项目

通过这两种方法，你可以让文字拥有漂亮的渐变效果，同时保持可维护性和响应式布局。